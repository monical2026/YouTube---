# YouTube 学习笔记插件测试方案

> 状态：测试方案草案，尚未开发、尚未执行测试。本文中的“预期结果”是验收要求，不是已验证结果。
> 测试依据：[实施计划](./PLAN.md)、[设计说明](./design.md)、[技术方案](./technical-design.md)及用户后续确认的需求。文件名或方案变化时同步修正引用。
> 执行记录统一填写到 [测试反馈](./test-feedback.md)。本文件不预填“通过”。

## 一、测试目标与边界

第一版在用户当前电脑和 Chrome 上验证完整的 YouTube 阅读与记录流程，重点排除记错视频、摘错时间、笔记丢失、个人修改被覆盖等问题。

- 已确认范围：仅根据逐字稿整理，不分析画面；个人使用；原文、中文、中英双语模式；中文 AI 内容；逐字稿联动、编辑、笔记、导出；视频切换后内容同步切换；关闭插件面板时仍可使用快捷键记录；非 YouTube 网页不显示插件界面。
- 已确认支持普通点播页面：`https://www.youtube.com/watch?v=VIDEO_ID`；Shorts、正在直播和站外嵌入明确留到后续。直播回放、移动版域名、音乐站点等不因返回文字就宣称已兼容；普通视频全屏与剧场表现仍需验证。范围外功能不用实现，但不误注入、不请求、不串数据的检查必须执行。
- “关闭插件”在本方案中指关闭面板，扩展仍安装并启用；扩展被停用或卸载时，不要求快捷键可用。
- 自动化替身用于稳定重现响应乱序、服务失败、时间边界等情形；真实视频与真实服务用于验证实际集成。两者分别记录，不能互相替代。
- 划词粒度、中文补齐、原文修改后译文处理、快捷键等按 design.md 和 technical-design.md 的明确设计默认验证，人工验收时再判断是否符合使用习惯；不能把实施默认写成用户已确认。若两文件没有规则或相互冲突，相关用例先阻塞并澄清。费用上限没有确认时不擅自进入付费处理。

## 二、优先级和结果定义

| 标记 | 含义 | 发布要求 |
| --- | --- | --- |
| P0 | 数据丢失、跨视频混入、私密信息泄漏、主要流程无法完成 | 必须修复并回归 |
| P1 | 核心功能或常用交互不符合已确认需求 | 必须修复并回归 |
| P2 | 边缘状态、视觉一致性或补充质量要求 | 纳入范围的用例仍必须完成；不能自行跳过 |

结果只允许填写：未执行、通过、失败、阻塞、不适用。

- **通过**：完成所有步骤，实际结果满足预期，有足够证据。
- **失败**：已执行，存在可复现的不符合项，关联问题编号。
- **阻塞**：缺少凭据、确认、可用样本或依赖，未获得实际结论。
- **不适用**：已有明确范围依据，并记录原因；不能用来掩盖未实现的已确认功能。
- 自动化通过不等于人工通过；模拟服务通过不等于真实服务通过；文档检查通过不等于产品测试通过。

## 三、执行环境与样本

### 1. 每轮必须记录的环境

- 操作系统及版本、Chrome 版本、扩展版本、构建标识及 Git commit；未提交时记录源码变更摘要或构建校验值。
- 屏幕分辨率、浏览器缩放、YouTube 浅色或深色模式、扩展主题、语言模式、快捷键实际绑定。
- 本机组件运行状态、版本、服务适配器和模型标识；只记录凭据是否配置，不记录凭据内容。
- 网络条件、缓存是否命中、测试账号与样本是否可访问、是否有广告；账号记录使用非敏感代号。
- 自动化依赖和命令由实际工程确定后填入，不提前编造可运行命令。

### 2. 样本清单

| 样本编号 | 内容 | 用途及要求 |
| --- | --- | --- |
| F01 | 人工制作的带时间英文字幕，附预期分段和中文译文 | 完整句、缩写、标点、Unicode、空白、时间边界；无需外部请求 |
| F02 | 碎片化自动字幕与重复、重叠片段 | 去重、合并、非负时长及时间映射 |
| F03 | 超长句、长停顿、缺少标点和较粗时间戳 | 分段容错与精度限制，不制造不存在的词级时间 |
| F04 | 视频 A、B 的可控响应，支持延迟、逆序、取消后继续返回 | SPA 导航、旧请求隔离及多标签竞态 |
| F05 | 外部服务响应替身 | 成功、异步任务、认证失败、限流、超额、超时、畸形响应、部分结果 |
| F06 | 原文和译文多个版本、已摘录笔记与未保存草稿 | 差异提示、更新引用、个人内容保护 |
| F07 | 含 HTML、脚本样式文本、Markdown 符号、异常文件名及伪指令的文字 | 安全呈现、导出、AI 不可信输入；全部使用测试字符串 |
| F08 | 大体量逐字稿和多条笔记 | 长列表及搜索、编辑、导出压力；先记录实际条数和体积 |
| R01 | 真实英文普通视频，具有可获取字幕 | 记录 URL、标题、时长、字幕类型、人工参考时间点 |
| R02 | 真实无可用字幕的普通视频 | 人工核实并记录字幕状态，用于外部转写；失败时如实记录 |
| R03 | 真实英文长视频 | 时长与类型由用户常看视频确定，未取得样本前保持待补 |
| R04 | 实际有广告或平台状态变化的视频 | 有条件时验证；无法稳定重现时先用替身覆盖，再记录真实覆盖缺口 |

真实样本可能失效。每轮先检查可访问性，不把视频被删除或地区限制记成插件已通过。仅保存测试所需的截图和文字片段；不得把完整个人笔记或凭据上传到公共仓库。

## 四、测试顺序与阶段出口

1. **静态检查与构建**：类型、格式、构建、Manifest 权限、依赖和敏感信息检查。失败时先修复，不开始发布验收。
2. **本地模块测试**：分段、时间定位、版本比较、存储、导出等稳定逻辑，使用固定样本。
3. **服务契约与组件集成**：验证请求、响应、语言、缓存、错误和乱序；模拟状态与真实接口分开。
4. **受控端到端测试**：可控播放器和响应替身覆盖竞态、广告状态、跨标签、崩溃恢复等难稳定复现的场景。
5. **真实 Chrome / YouTube 测试**：加载实际扩展，使用真实服务完成有字幕与无字幕流程，保留证据。
6. **人工验收**：用户亲自在当前电脑上完成核心使用流程，对指定构建明确反馈是否通过。
7. **问题修复与回归**：每个问题记录复现、原因、变更和回归结果；修复涉及已验收行为或更换构建时，重新取得该构建的人工确认。
8. **Git 与 GitHub 交付**：满足本文最后的门槛后再提交和推送。

## 五、具体测试用例

### A. 页面范围、入口与导航

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| NAV-01 | P1 | 在扩展启用状态打开普通 YouTube 视频页，检查视频下方入口，点击并关闭面板，再次打开 | 只有一个小入口；位置不遮挡视频、标题或原生操作；可反复打开，不重复挂载 |
| NAV-02 | P0 | 打开搜索引擎、文档站点和本地页面，尝试预定快捷键 | 不注入面板、浮钮、笔记弹窗或样式；不为这些页面调用字幕或 AI 服务 |
| NAV-03 | P1 | 从视频页进入 YouTube 首页、搜索、频道页，再返回视频 | 非支持页面移除视频专用 UI 并停止播放绑定；返回后按当前视频重新建立上下文 |
| NAV-04 | P0 | 视频 A 完成加载后，点击站内推荐跳到视频 B；用前进、后退返回 A | 逐字稿、AI、笔记、时间及高亮均对应当前视频；无需手动刷新；可复用各自缓存 |
| NAV-05 | P0 | A 请求延迟时切 B，再切 A；让第一次 A 响应最后返回 | 仅当前有效任务可更新当前界面；旧响应不能覆盖新的编辑、状态或新一轮 A 数据 |
| NAV-06 | P0 | 同时打开 A、B 两个标签，交替激活、播放和按快捷键 | 快捷键只作用于当前有效标签；数据按视频隔离；后台标签响应不改变当前笔记弹窗 |
| NAV-07 | P0 | 在 A 的笔记弹窗写未保存内容，随后站内跳到 B，再执行保存或关闭 | 草稿保持绑定创建时的 A、时间和摘录；不保存进 B；离开时按确定的草稿恢复规则处理 |
| NAV-08 | P1 | YouTube 页面局部重绘、窗口缩放、切影院模式后再次打开 | 入口和面板不重复、不消失；卸载的旧事件监听不会造成多次跳转或多条笔记 |
| NAV-09 | P1 | 普通视频→Shorts→watch URL 正在直播→普通视频，以及站外嵌入；各处按快捷键 | 范围外不显示入口或笔记窗、不获取字幕或触发 AI；及时清理旧 UI，保留草稿；返回普通视频恢复正确内容，不仅按 watch 路径误判直播 |
| NAV-10 | P1 | 同一视频改变播放列表参数或通过带时间链接定位 | 仍归属于同一视频；不重复抓取整份逐字稿，笔记和缓存保持正确关联 |

### B. 获取、服务契约与加载

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| API-01 | P0 | 运行有字幕真实样本，记录响应类型、语言、时间单位及转换结果 | 正确关联视频 ID；原文、起始时间和时长可用于同步；真实调用证据不含凭据 |
| API-02 | P0 | 运行无字幕真实样本，完成允许范围内的转写和结果读取 | 明确区分原生字幕、生成字幕与任务等待；转写完成后可播放定位；失败不展示伪造稿件 |
| API-03 | P1 | 用替身返回异步任务、排队、处理中、完成，以及任务失败 | 只展示真实阶段；轮询有间隔和上限；完成读取一次；失败可理解并可重试 |
| API-04 | P1 | 分别返回未知进度及可计算翻译段数 | 未知进度用不确定进度条；已知进度按真实完成量计算；不会假装达到 100% |
| API-05 | P0 | 返回无凭据、无效凭据、额度不足及 429，反复点击重试 | 给出对应中文状态；不无限循环、不自动进入付费；尊重可用重试信息并防重复请求 |
| API-06 | P1 | 模拟离线、请求超时、服务 5xx、本机组件请求中崩溃，恢复后重试 | 已保存内容仍能访问；错误不清空笔记；恢复后可继续，不产生重复摘录 |
| API-07 | P0 | 并发打开同一视频、重复点击加载，在等待中切页 | 不因重复操作重复创建同一处理任务；迟到结果只写正确视频和版本；记录实际调用次数 |
| API-08 | P1 | 返回缺失文本、非法时间、乱序片段、意外语言或部分结果 | 校验失败或明确降级；不崩溃、不把非中文标成译文；部分结果状态不冒充完整 |
| API-09 | P1 | 请求语言不可用并发生服务回退 | 根据实际返回语言标识处理，不假定请求中文就已取得中文翻译 |
| API-10 | P1 | 用已处理视频再次进入，随后修改本地译文并再次进入 | 可复用对应缓存；不多扣额度；本地手动修正不被缓存刷新或迟到翻译覆盖 |
| API-11 | P1 | 组件未安装、版本不兼容、启动失败及正常空闲时打开设置并获取字幕 | 区分安装与启动错误；空闲自动按需启动，不误报离线；不要求配对码，旧笔记仍可编辑 |

### C. 分段、翻译与显示模式

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| TXT-01 | P1 | 输入 F01–F03，与人工确定的预期边界比较 | 尽量保留完整句；合并碎片且不丢词、不重复词；过长段落有控制；时间单调且有效 |
| TXT-02 | P0 | 分段后逐项追溯原字幕时间，再编辑某段文字 | 稳定来源关联保留；文字变长或变短不会按字符比例随意改写原有时间 |
| TXT-03 | P1 | 原文模式首次启动且没有翻译缓存 | 获取原文后即可阅读，不为整段中文翻译等待；界面只显示原文 |
| TXT-04 | P1 | 依次切换中文、双语、原文，覆盖翻译未完成和完成状态 | 只展示所选模式；双语在对应段内成对显示；模式切换不重复发起同样任务 |
| TXT-05 | P1 | AI 内容已生成后连续切换三种逐字稿模式 | AI 保持中文和原有内容，不自动重新生成；通过请求记录核实 |
| TXT-06 | P1 | 翻译包含人名、术语、数字、否定、方法步骤的参考段落 | 人工核对意思、数字、否定关系和术语；错误逐条记录，不用主观“整体不错”替代结果 |
| TXT-07 | P0 | 编辑中文，在保存同时让旧翻译响应到达，刷新页面 | 保存的中文保持；不发起自动重翻译，不被旧响应覆盖 |
| TXT-08 | P1 | 编辑原文并查看已有译文，再主动重译 | 按设计默认保留原译文并标记待核对；只有主动操作才重译；人工验收判断此默认是否符合预期 |
| TXT-09 | P1 | 测试没有字幕的时间空隙、纯音乐、超出最后一段的时间 | 不高亮无关内容，不伪造补全文字；粗时间戳的精度限制记录清楚 |
| TXT-10 | P1 | 搜索中英文关键词，切换上一条/下一条，再点击命中段落 | 搜索只覆盖实际取得的文字，数量准确；定位命中不改变播放时间，点击段落才跳转播放 |

### D. 播放联动、自由浏览与选词

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| PLAY-01 | P1 | 以 1×、0.5×、2× 播放，观察段落起止边界并拖动进度条 | 高亮按实际视频时间更新，倍速不会累计漂移；边界无重复高亮 |
| PLAY-02 | P1 | 开启跟随后持续播放，再用滚轮、触控板和滚动条浏览远处 | 跟随时当前段可见；手动浏览进入自由状态；高亮继续更新但列表不被拉回 |
| PLAY-03 | P1 | 在播放和暂停两种状态下自由滚动后点击底部“跟随” | 列表回到当前实际时间段，恢复跟随；点击跟随不改变视频时间或暂停状态 |
| PLAY-04 | P1 | 单击历史或未来段落文字，包括视频原为暂停状态 | 跳到该段起始时间并开始播放，高亮立即一致；播放被浏览器拒绝时有实际状态反馈 |
| PLAY-05 | P0 | 拖动选择一段及跨段文字，释放后点击“做笔记” | 选词不触发视频跳转；入口不丢失选区；摘录内容符合已确认的选区和双语对齐规则 |
| PLAY-06 | P1 | 在逐字稿编辑框、笔记编辑框及图标上点击和选词 | 编辑或操作按钮不冒泡触发段落播放；键盘输入不意外控制 YouTube 播放 |
| PLAY-07 | P0 | 模拟并实测广告进入/结束期间的高亮、跟随、快捷键和跳转 | 不将广告时间写为正片笔记；无法确认正片位置时暂缓操作并提示；恢复后重新读取正片时间 |
| PLAY-08 | P1 | 使用键盘翻页或聚焦逐字稿，滚动面板外页面 | 自由浏览与跟随切换基于实际阅读操作；页面滚动不会错误修改笔记时间 |

### E. 笔记创建、编辑与版本更新

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| NOTE-01 | P0 | 面板打开时按快捷键，输入理解和疑问，保存 | 摘录触发瞬间的播放片段，固定视频和时间；双语、个人文字和来源完整保存 |
| NOTE-02 | P0 | 关闭面板后播放并按快捷键；另测从未打开面板且尚无字幕的情况 | 可直接进入笔记流程；已缓存时可摘录；未加载时显示获取/补齐状态，不编造内容；等待期间时间和视频保持绑定 |
| NOTE-03 | P0 | 快捷键创建后等待较久再保存，其间继续播放、暂停及拖进度条 | 摘录仍属于触发时的位置；个人想法不被后续当前段替换 |
| NOTE-04 | P1 | 原文模式无中文时摘录，再模拟译文成功、失败和稍后重试 | 先保存来源、时间与草稿输入，成功后补齐引用；失败可重试，不覆盖人工修正；未完成译文不冒充成功 |
| NOTE-05 | P1 | 三种显示模式中分别选词摘录，含跨段选区 | 按确认规则保存双语及所选范围；不能把整段译文伪装成精确选词翻译；必要的上下文可区分 |
| NOTE-06 | P0 | 保存笔记后重新打开编辑，修改想法、疑问和允许编辑的引用字段，再刷新 | 改动持久保存，其他字段不丢失；取消编辑不覆盖已保存版本 |
| NOTE-07 | P1 | 留空理解或疑问、输入中文长文、多行、表情及英文，保存后再编辑 | 选填项不强制输入；文本原样保存；中英文无需常驻标签；图标可辨认且有辅助名称 |
| NOTE-08 | P0 | 笔记保存后修改对应原文或译文，再查看笔记 | 摘录仍保留旧版本；显示来源更新提示，可查看真实差异；不静默替换 |
| NOTE-09 | P0 | 对来源更新分别选择“保留原样”和“更新引用” | 保留操作不改摘录；更新只更新选定来源引用；两种操作均不改个人理解、疑问及其他笔记 |
| NOTE-10 | P0 | 来源连续修改两次，在差异窗口打开期间再修改；测试含用户手改引用的笔记 | 识别版本变化，避免按过期差异覆盖新数据；冲突明确可处理，不能静默丢失手改内容 |
| NOTE-11 | P0 | 同视频两个标签同时编辑同一笔记，再关闭其中一个 | 采用已确定的版本冲突处理；不得无提示覆盖较新编辑；两标签最终状态可解释 |
| NOTE-12 | P1 | 连续重复快捷键、输入框中触发快捷键、快捷键未绑定或被占用 | 当前视频已有未完成窗口时聚焦旧草稿，不替换摘录或创建新条；有可发现的入口和实际绑定提示 |
| NOTE-13 | P1 | 在笔记里手改双语引用，然后查看源逐字稿；保留一次来源更新后再次打开，再让来源更新 | 手改仅影响该笔记；保留后不重复提醒同一来源版本；新的来源版本仍可提醒 |
| NOTE-14 | P0 | 编辑旧笔记，等待草稿保存，再取消修改；另测关闭窗口后重新打开并完成 | 草稿保存不提前改变正式笔记；取消回到编辑前版本；关闭保留草稿，完成后才提交新修订 |

### F. 保存、恢复与导出

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| DATA-01 | P0 | 分别在 A、B 保存笔记和修正文本，刷新并完全退出重启 Chrome | 各视频数据完整恢复、隔离正确；AI 和稿件缓存状态准确；无需依赖面板始终打开 |
| DATA-02 | P0 | 保存完成后停止扩展后台运行并等待其恢复，再读取和修改 | 已确认保存的数据不丢失；一次操作不重复写入；后台生命周期不决定笔记寿命 |
| DATA-03 | P0 | 模拟存储写入失败、空间不足，在草稿未保存时关闭面板或切视频 | 不显示虚假的“已保存”；尽量保留草稿并提示可重试；已保存数据不受损 |
| DATA-04 | P1 | 无网络、组件不可用时打开已有视频记录 | 已保存笔记和缓存可读、可编辑；只有依赖外部处理的动作报错 |
| DATA-05 | P0 | 分别导出原文、中文、双语完整逐字稿，检查第一段、最后一段及总数 | 导出包含完整目标范围，遵守所选语言和时间戳设置，不能只导出屏幕可见部分 |
| DATA-06 | P0 | 导出含多条笔记、未补齐译文、旧版摘录和个人编辑的文件 | 内容对应实际保存状态，双语、理解、疑问、来源和时间不丢失；未完成或旧版状态不被误标 |
| DATA-07 | P1 | 在文本编辑器中打开 Markdown/TXT；点击来源时间链接 | 编码正确，可正常阅读；来源回到正确视频时间；不宣称已直接写入 Obsidian |
| DATA-08 | P1 | 导出 F07 的标题及正文，含路径字符、HTML、Markdown 和超长文件名 | 文件名安全、正文不会执行脚本；不改写个人原意；导出文件不含密钥或内部调试信息 |
| DATA-09 | P0 | 以旧版本地数据运行新构建并验证数据迁移，再模拟迁移失败 | 正常迁移保留数据和版本关系；失败不破坏原数据，有恢复路径；无旧版时记录不适用依据 |
| DATA-10 | P1 | 在导出窗口单选稿件、笔记或 AI 内容，并更改语言与时间戳选项 | 只导出所选内容；未完成内容状态清楚；AI 梳理单独导出时仍保留来源和生成性质 |

### G. AI 内容质量与可信度

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| AI-01 | P1 | 对真实视频生成摘要和内容结构，人工与视频核对 | 简体中文；结构含标题、时间范围、介绍、问题与应用；不以“第一章”等编号代替有效标题 |
| AI-02 | P1 | 检查各条金句、方法、时间范围并逐一回看 | 金句能找到原话；方法有来源和必要条件；无证据时不凑数；时间不超出视频 |
| AI-03 | P1 | 检查讲者明确场景和模型延伸场景、切片建议 | 明确区分推导与原话；固定标注基于逐字稿；第一版粗判断含主题回看时间及理由，时间不超出视频，主题范围不冒充精确切片范围；无适合片段时不凑数；不提供剪辑或视频下载操作，不声称已验证画面或生成视频文件 |
| AI-04 | P0 | 将 F07 的伪指令作为视频文字输入，返回含 HTML 或不合结构的内容 | 视频文字不能改变系统职责、读取本地秘密或执行操作；输出校验后安全展示 |
| AI-05 | P1 | 模拟长视频分批处理、部分失败、摘要失败及用户重新触发 | 不把部分覆盖当整片总结；保留有效结果并标注状态；重试不覆盖个人笔记 |
| AI-06 | P1 | 原文修改后打开已有 AI 内容，切换语言和视频 | 按确定的来源版本策略呈现；不静默重生成或混入其他视频；需要刷新时由明确操作触发 |
| AI-07 | P1 | 将摘要条目、金句或方法加入笔记，补充理解后保存 | 创建带真实来源的可编辑引用草稿，标明 AI 整理性质；不把机器输出填进“个人理解”冒充用户输入 |
| AI-08 | P1 | 使用含“点击这里”、未口述按钮名或参数的教程逐字稿生成梳理，检查请求与结果 | 不采集或发送画面；不调用视觉接口；不虚构画面中的按钮、参数与步骤，缺失细节标为逐字稿未提供；普通翻译与笔记仍可用 |

### H. 布局、可访问性与性能

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| UI-01 | P1 | 在用户实际窗口尺寸和较窄窗口检查视频下方入口、面板、笔记弹窗 | 视频关键操作不被遮挡；内容不横向溢出；底部跟随与保存始终可达 |
| UI-02 | P1 | 浅色、深色主题及 100%、125%、150%、200% 缩放下核对设计稿 | 字体、字号、行高、间距符合 design.md；中英文易读；高亮、焦点、选区和错误清晰可辨；实际测量正文对比度至少 4.5:1、必要控件和焦点至少 3:1 |
| UI-03 | P1 | 仅用键盘打开面板、切页、摘录、编辑、保存、关闭弹窗 | 焦点顺序合理；弹窗管理焦点并可退出；图标有辅助名称，悬停和聚焦可见中文提示 |
| UI-04 | P1 | 在笔记页查看双语、理解和疑问图标，输入长内容 | 无用户已要求移除的常驻文字标签；两类个人内容能辨认；长文可编辑且不截断丢失 |
| UI-05 | P2 | 在视频 A/B 连续切换至少 20 次后检查入口、监听和 UI 响应 | 无重复入口、重复笔记、明显累积卡顿或旧内容残留；记录样本条件 |
| UI-06 | P1 | 拖动面板宽度并重开，改变系统减少动态效果设置，再切换主题 | 宽度偏好按设计保存并遵守窄屏下限；减少动态效果时不强制平滑滚动；主题切换不影响稿件和笔记 |
| UI-07 | P1 | 全屏视频内按快捷键，退出全屏查看记录 | 真实验证浮层能力；不能可靠呈现时至少保留触发时刻草稿并在退出后提示；记录能力限制，不虚称支持全屏浮层 |
| PERF-01 | P1 | 在已缓存且无需服务处理时，记录快捷键到可输入弹窗、点击跟随到定位的耗时 | 拟定目标均为 300 ms 内；属于待实测验收建议，首次真实测量后评估并记录，不可写成现有表现 |
| PERF-02 | P1 | 播放 F01/F08，记录时间边界与 UI 高亮更新时间差及交互耗时 | 拟定高亮更新延迟目标 300 ms 内，不包含源字幕时间误差；长列表无持续阻塞输入 |
| PERF-03 | P2 | 真实有字幕、无字幕、翻译与 AI 各至少记录一次分阶段耗时 | 记录实际值、视频长度及缓存状态；外部服务总等待暂无硬承诺，不编造即时生成能力 |

### I. 权限、凭据与本机组件

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| SEC-01 | P0 | 检查 Manifest、构建包、配置、源码、浏览器存储、导出与日志 | 日常密钥存 macOS 钥匙串；开发环境变量不进入扩展包；浏览器持久数据、日志、命令参数和响应无密钥；示例仅占位符 |
| SEC-02 | P0 | 检查 URL 限制并从非支持页面、错误发送方调用消息入口 | 拒绝越界来源与无效视频请求；无不必要全站权限；校验来自页面的消息和内容 |
| SEC-03 | P0 | 向组件发送异常操作、路径、任意 URL、请求头及超大或畸形 Native Messaging 帧 | 校验操作、配置引用、帧长度与负载；拒绝任意网络代理、文件读取和命令执行，协议错误明确失败 |
| SEC-04 | P0 | 检查网络与诊断记录，执行转写、翻译、AI、笔记保存 | 请求仅包含完成对应功能所需信息；个人理解与疑问不自动发送外部模型；无密钥回显 |
| SEC-05 | P0 | 使用 F07 文本渲染逐字稿、AI、笔记及差异窗口 | 按文本安全显示；不执行脚本、事件属性或不可信链接协议 |
| SEC-06 | P0 | 未登记扩展 ID、伪造调用来源、内容脚本伪装设置页访问组件 | 主机登记限制 allowed_origins，组件和后台均验证调用；拒绝越权保存或读取凭据，无秘密回传 YouTube 页面 |
| SEC-07 | P0 | 首次保存 LLM 与 Supadata 密钥，完全退出 Chrome 后重启并换视频调用 | 凭据与服务正确对应；无需重填 API key 或插件解锁密码；记录系统授权提示的真实次数，不预设零弹窗 |
| SEC-08 | P0 | 钥匙串锁定、系统拒绝授权或原生保存失败，再解锁重试 | 系统密码仅在系统窗口输入；不降级明文，不覆盖旧配置，不丢笔记；恢复后可重试 |
| SEC-09 | P0 | 更新组件版本或签名、开发与正式扩展 ID 切换、主机协议不兼容 | 不放宽凭据访问权限；版本不兼容给出修复入口；已有凭据迁移或再次授权有明确步骤和实测记录 |
| SEC-10 | P1 | 检查安装交付与空闲、调用、浏览器退出时的组件生命周期 | 用户无需装开发工具或手动启动；无 HTTP 监听和开机常驻任务；请求后按策略退出，关闭浏览器不承诺继续处理 |
| SEC-11 | P0 | 翻译中退出 Chrome、供应商接收请求后组件崩溃，再打开视频继续 | 已保存笔记与批次可恢复；先查原任务，未知计费结果不盲重发；无重复扣费承诺，明确远端可能继续执行 |

### J. 独立设置网页与 LLM 连接

依据 [服务设置方案](service-settings.md) 和 [文件架构](architecture.md)，新增以下用例；均尚未执行。

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| SET-01 | P1 | 从面板齿轮、Chrome 扩展选项及没有视频的情况下打开设置 | 打开同一独立完整设置页，不依赖 YouTube；其他网页不被注入 UI；窄屏导航及保存可用 |
| SET-02 | P0 | 新建 LLM 连接，填地址、key、模型并保存；关闭重开 | 非敏感资料正确恢复，key 只存凭据库，页面仅显示已配置；空 key 保存保留旧值 |
| SET-03 | P1 | 在当前表单草稿测试连接，分别模拟列模型成功但生成失败、模型列表不存在、正常生成 | 分别展示网络、鉴权和模型能力；列模型不等于调用成功；允许手动模型 ID，测试不自动保存 |
| SET-04 | P0 | 模拟新 key 无效、凭据库拒绝、配置写入失败，期间已有可用连接 | 不覆盖旧配置，不假报保存成功，不降级成明文文件；敏感输入不保存草稿或出现在日志 |
| SET-05 | P1 | 主动 LLM 翻译和分析共用连接后改为分别使用不同模型，启动真实小型任务 | 任务实际到所选服务/模型，个人理解和疑问不外传；结果记录对应配置版本 |
| SET-06 | P0 | A 配置任务运行中保存 B 配置；再启动新任务并检查已有结果 | 旧任务固定 A，新任务使用 B，旧结果保留；不自动整库重译或混用旧鉴权；过期测试标记失效 |
| SET-07 | P0 | 从内容脚本伪造设置来源，调用保存配置/测试任意地址/读取凭据 | 后台按真实 sender 拒绝；设置页不要求 videoId，但必须是登记的扩展页面；没有读回完整 key 接口 |
| SET-08 | P0 | 输入含用户名密码、query key、私网、元数据地址及跳转到内网的 Base URL；测试更换目标主机 | 非法地址拒绝；普通任务不能传任意 URL；跨主机不自动发送旧 key；本地例外仅作用于登记端口 |
| SET-09 | P1 | 配置 Supadata，切换字幕模式，测试未知余额与真实最小调用 | 字幕和 LLM 凭据不混用；明确测试消耗，估算不是官方剩余额度；不虚构无费用健康检查 |
| SET-10 | P0 | 修改环境变量来源密钥、停用使用中连接、清除凭据及取消删除 | 环境变量来源只读；影响与确认明确，取消不更改；新任务不使用已撤销凭据，不删除旧笔记 |
| SET-11 | P1 | 两个设置页同时编辑一份配置，组件不可用时保存后恢复 | 版本冲突明确，失败不假报保存成功；陈旧快照不覆盖新配置，组件正常空闲不算不可用 |
| SET-12 | P1 | 检查工作区依赖、构建输出和共享模块；仅配置字幕而不配置 LLM | 内容脚本不导入服务密钥模块，shared 无平台副作用；原文/本地笔记可用；本地翻译支持时无需 LLM key，主动 LLM 翻译和分析提供设置入口 |

## 六、人工端到端验收脚本

人工必须使用实际扩展和真实视频完成，不能只看自动化截图。依次执行并逐项记结果：

1. **HUMAN-01｜进入与阅读**：从普通视频下方小按钮打开，观察加载，切换三种模式，核对是否只展示当前模式；听一段英文并检查中文是否帮助理解。
2. **HUMAN-02｜播放联动**：播放、暂停、手动翻到远处、点击“跟随”、点击另一段文字；确认行为与预期一致且不打断自由阅读。
3. **HUMAN-03｜两种摘录**：关闭面板按快捷键记一条，打开后划词记一条；分别写理解和疑问，再修改已保存笔记。
4. **HUMAN-04｜来源纠错**：修改一段译文，查看旧笔记差异；分别试保留和更新，确认自己的文字不变。
5. **HUMAN-05｜视频切换**：A→B→A，等待中切换一次，两个标签交替记录一次；确认每条笔记、稿件和 AI 属于正确视频。
6. **HUMAN-06｜保存与带走**：刷新、重启 Chrome、导出完整稿件和笔记，检查文件实际内容与时间链接。
7. **HUMAN-07｜AI 与无字幕**：核对真实 AI 总结、结构、金句、方法及切片建议；验证一个真实无字幕视频，不能以替身代替。
8. **HUMAN-08｜使用感受**：在用户常用窗口和主题下评价字号、行距、入口位置、弹窗大小及操作负担，记录具体不顺手的步骤。
9. **HUMAN-09｜配置服务**：从齿轮进入独立网页，完成组件安装检测、钥匙串授权、LLM 及 Supadata 配置、指定模型连接测试、任务模型选择和保存；重新打开确认持久化，执行短翻译和 AI 请求核实真实生效，反馈设置是否易懂；完全退出并重启 Chrome，换视频再次调用，确认无需重填 API key，记录实际系统提示次数及场景。

人工验收记录必须包含日期、测试人、对应构建、通过/未通过/待确认，以及用户实际反馈原文或可定位的证据。没有明确反馈时保持“待确认”。

## 七、反馈与问题闭环

- 每轮复制 test-feedback.md 中空白轮次模板，不覆盖既有记录。
- 每个失败绑定用例、复现步骤、期望、实际、影响范围和证据；未查明的原因写“待定位”，猜测写“假设”。
- 修复记录写文件、变更目的、关联版本及副作用风险。只记录实际修改，未修复不写完成。
- 回归先重跑失败用例，再验证受影响相邻流程；数据和导航问题必须同时回归跨视频、跨标签及保存恢复。
- 保留未解决、阻塞、不适用项及下一步，不能只列已经通过的用例。

## 八、提交与推送门槛

用户已提出“全部测试做完且人工测试通过后再提交并推送 GitHub”。这是后续满足条件后的交付动作；当前不提交或推送。

- [ ] 所有适用用例已执行且通过；范围外条目有明确依据；无以未执行或阻塞冒充通过的情况。
- [ ] 真实有字幕、无字幕和 AI 服务验证完成，费用与凭据条件允许；模拟通过未替代真实结果。
- [ ] 所有问题有实际修复与回归证据，反馈文档与测试结果一致。
- [ ] 用户对最终候选构建明确人工验收通过；记录签收构建校验值或对应 Git commit。
- [ ] 人工验收后代码或打包产物变动，先回归并重新确认新构建；文档记录追加也应可追溯。
- [ ] 确认目标 GitHub 仓库、账户/组织、可见性、分支与远端 URL。当前尚无已确认 remote；不得擅自创建公开仓库。
- [ ] 检查 `.gitignore`；`.env`、私钥、凭据文件、个人测试数据及敏感截图不纳入提交；无真实值的 `.env.example` 可纳入。
- [ ] 暂存后检查 `git diff --cached`，确认提交范围正确且无真实密钥、token、密码、私钥；检查结果不输出秘密值。
- [ ] 提交内容精确对应经过测试和人工签收的源码/构建，不夹带未测改动；首次提交后把实际 commit 与签收构建建立关联。
- [ ] 使用普通提交和推送，遵循 `codex/` 分支前缀；不使用强制推送、不删除或覆盖用户已有历史。
- [ ] 推送后核实远端分支指向预期 commit，记录仓库链接、分支、commit 和时间；推送失败如实记录，不写“已上传”。

仓库目标与可见性属于待确认项；用户人工测试未通过或未进行时，Git 提交/推送门槛保持未满足。

## 八、本地翻译与费用边界补充用例

以下用例尚未执行；必须在用户实际 Chrome 验证，模拟接口不能替代本地能力验证。

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| LOCAL-01 | P0 | 扩展来源文档内检查 API、英中语言对、用户激活、下载进度与首次翻译 | 记录真实 Chrome 版本和执行环境；不假定后台 Worker 支持；英文对应简体中文，下载失败可重试 |
| LOCAL-02 | P0 | 中文/双语、本地模型不可用或下载失败、未配置 LLM 场景检查请求 | 本地支持时可翻译，不消耗云端 LLM token；失败不自动云端回退，草稿不丢 |
| LOCAL-03 | P0 | 当前段与全文分别主动调用 LLM，期间编辑译文再应用候选 | 请求范围符合点击；候选需选择应用，修订冲突不覆盖手改，笔记快照不自动更新 |
| LOCAL-04 | P1 | 原文模式、双语摘录、面板未打开、切视频、文档关闭和浏览器重启 | 只翻译必要内容；激活不可用有提示；恢复和迟到响应不串视频，不丢个人记录 |
| LOCAL-05 | P1 | 切换本地与 LLM 引擎、重复打开视频、改变原文修订 | 缓存按来源及引擎隔离；不重复同一任务，人工译文不会被缓存刷新覆盖 |
| LOCAL-06 | P0 | 已有字幕与无字幕视频分别获取，检查供应商请求模式与额度 | 优先已有字幕，无字幕不自动生成；生成需明确选择，缓存复用；失败和重复调用按实测记录 |

## 九、无字幕生成确认用例

| 编号 | 优先级 | 步骤 | 预期结果 |
| --- | --- | --- | --- |
| COST-01 | P0 | 无字幕时检查提示，分别取消、关闭及确认 | 展示视频、时长、预计积分和依据；只有确认发生成请求，关闭与取消不提交 |
| COST-02 | P0 | 快捷摘录且面板关闭，遇到无字幕后取消生成 | 时间与个人输入已保存，引用可待补齐，不自动生成或丢失草稿 |
| COST-03 | P0 | 估算未知、视频切换、规则或时长变更，再点旧确认 | 未知不能提交；陈旧确认不能授权新视频或变更估算，重新展示确认 |
| COST-04 | P0 | 双击确认、多标签重复操作、提交结果未知、恢复已有 jobId | 去重有效；未知不盲重发；查询原任务和缓存不重复确认，新生成重新确认 |
| COST-05 | P0 | 设置允许生成并执行无字幕连接测试，检查实际请求 | 设置不能跳过逐次确认，已有字幕查询不用 auto；测试生成同样先估算确认 |

## 扩展更新后的恢复回归

1. 加载最新构建并刷新 YouTube 视频页，确认视频下方学习笔记入口出现。
2. 保持视频页打开，重新加载扩展；旧页面不再持续发送失效请求，也不新增未捕获的 Extension context invalidated 错误。
3. 刷新视频页，确认入口恢复并可打开面板；已保存笔记和设置保留。
4. 历史错误须先记录原因与堆栈，再清除后观察新增记录；只清日志不能作为修复通过证据。

## 实际视频流程回归补充

1. 重新加载扩展并刷新 YouTube，打开面板，不点获取与本地翻译按钮：应自动抓取并翻译，进度页覆盖内容区，结束后显示双语。缓存完整时不重复翻译。
2. 切换新视频及往返旧视频，内容不串片；原文模式不自动翻译；本地失败显示重试，不能自动调用 LLM。
3. 当前段及全片 LLM 翻译：未手改段落逐批直接更新；手改／请求期间变化的段落保留原结果并显示对照。
4. 40 分钟至 1 小时以上视频整理：全部段落按批次覆盖，最终主题按时间排序，金句与方法可追溯；检查真实输出质量。
5. 主面板停在笔记页，用快捷键输入理解、疑问并完成：笔记立即出现在主面板；关闭重开仍保留；翻译进行中保存笔记也不得丢失。

## 0.1.1 人工回归

1. 扩展管理页重载后确认版本为 0.1.1；已开 YouTube 页不刷新，使用页面入口和扩展菜单图标均可打开。
2. 连续收起／展开 10 次，收起后有名称横条，展开后内容及笔记保留；切换视频后重试，不显示旧视频内容。
3. 笔记列表、草稿、快捷弹窗和普通编辑窗口分别删除单条笔记；取消不删除，确认后列表及重开后均消失，其他笔记不变。
4. 用所选 LLM 整理长视频：时间来自字幕范围，金句原文可定位；部分无效引用应有提示，不能伪造来源。不同模型分别验收真实生成质量。
5. 展开面板没有“学习笔记”大标题和常驻本地翻译按钮；独立阅读可返回来源视频。原标签已关或切换时不能覆盖用户其他页面。

## 0.1.2 回归

1. 同一视频打开两次及重新启动 Chrome，已有字幕与译文直接显示；后台请求计数不应增加。首次抓取途中关闭面板，后台完成后重开应命中缓存。
2. 双栏顶部与视频对齐，窄屏及影院模式不覆盖播放器；版本只在管理页查看。加载图标、文字、进度条组成整体居中，小窗口可滚动且元素不变形。
3. 笔记四种正文同字号，长理解／疑问换行后保持图标在首行左侧；编辑／删除同时靠右。
4. 长视频整理完成后首段为约 100 字简短总览；精简失败详细主题仍保留，长总结默认折叠。
5. 分段逻辑及既有段落未重写，等待用户确认新规则。

## 0.1.5 回归

1. 双栏顶部与播放器对齐，影院和窄屏不覆盖视频；右下角跟随点击定位当前段，手动滚动仍暂停自动跟随。
2. 中文／英文／中英直接切换；仅一个 LLM翻译按钮调用全片翻译，独立阅读仍可打开。
3. 导出分别单选、组合与全选三类内容；空选禁止导出，TXT 无 Markdown 标题标记。DOCX 可解压并在 Word 中核对中文、换行、顺序；PDF 在 Chrome 打印窗口另存并检查分页。
4. 全片简短总结独立显示，详细介绍只在各主题处；旧长总结不顶满总览。


## 0.1.7 纯规则分段专项验收

1. 重新加载 extension/dist 扩展并刷新普通 YouTube 视频。新视频自动使用新规则；旧视频点击“重新分段”，核对预览后应用，按需点击“补齐本地翻译”。
2. 对照已提供截图，检查姓名、欢迎语和 number E. 不再残留为半句；同时检查原本完整的短句、长句和引语不被破坏。
3. 缺标点、口头重复、重叠字幕时间和多人访谈各选实际样本；核对不漏字、不重字、不把源字幕间隔误认作真实停顿，不宣称已识别没有标签的说话人。
4. 验证取消不改内容；应用后重启仍保留；恢复带回旧原文和译文；手改段及个人笔记保持原样；另一窗口更新时拒绝旧预览覆盖。
5. 点击新段跳转、跟随及高亮采用来源时间精度；没有词级时间时，多个新段可能共享时间，不按词数平均分配时间。
6. 本地组件夹具可用 `pnpm exec vite --config tests/browser/vite.config.ts --host 127.0.0.1 --port 4173 --strictPort` 启动，打开 `/tests/browser/segmentation.html`。此夹具只验证算法和弹窗交互，不代替真实 Chrome 扩展、数据库或服务验收。


## 0.1.10 AI 梳理验收补充

1. 用真实视频重新整理，检查每段场景是否有具体处境与用法、来源标记；切片总判断和各段结论是否回答值得切及独立性，不能只有反差等赞美。
2. 核对知识点的理解、作用和每个来源时间，跨批同名知识合并时间；前置知识无必要不凑数。
3. 金句四种类型均不得编造原文；跨段摘录仍是连续原文；点击时间跳转。默认小图标复制双语，菜单复制中文／原文；拒绝剪贴板权限时提示失败。键盘可操作，明暗主题均可见。
4. 旧记录可读且提示重新整理；新内容导出完整；视频切换和已有保存校验继续适用。
5. 长视频分批主题、知识、金句不可丢失。模型超时保持旧结果，不能用模拟数据替代真实输出质量验收。


## 0.1.11 模型结构容错回归

- 模型返回“方法洞察”等未约定分类：有效金句与其他主题保留，只省略分类并给警告。
- 分类带首尾空白：仅去空白；不存在的来源及改写原文仍不能通过。
- 知识清单来源列表类型错误：明确栏目、序号和字段，不回显原始回复。
- 非 JSON：固定错误，不泄漏模型文本。
- 用真实失败回复回放；用户指定视频分批请求，另经安装后的 Native Messaging 协议检查响应。


## 0.1.12 最终人工复测

1. 重载扩展／刷新 YouTube，金句一键复制双语、中文、原文，核对系统粘贴结果，不能要求手选。独立阅读也要验证。
2. 逐字稿和脉络分别划词，操作条在选区下方；滚动消失，note 保留选中文字及来源，AI提问先存草稿。
3. 知识来源时间紧凑展示，展开其他来源后核对真实位置；知识点可加入笔记。
4. 笔记直接提问、追问、仅保存问题；刷新后内容仍在，导出包含问题、AI回答和个人理解且标记区分。
5. 模拟保存失败时不调用模型；模型失败保留问题；切换视频不回写迟到回答，答复保存失败能重新保存。
6. 主题时间标题同排，三个加粗栏目分条且切片价值明确，底部脉络位于跟随左；分段／补译测试按钮消失。


## 0.2.0 回归补充

- 从逐字稿、脉络和知识清单划词，笔记只有一次选中摘录；提问及导出不扩展整段逐字稿；知识点标题保留为问答上下文。
- 输入理解与疑问后取消，重新打开和刷新均保留；完成结束草稿；模拟写入失败保留编辑器及输入。
- 窄面板四个底部操作同排，模块标题 16px，小标题及正文 15px，知识标签字重 600。
- 五类金句存取及旧两类标签映射；未知标签仍按既有规则提示，来源原文校验保持。


## 0.2.1 补充回归

1. Chrome 真实扩展检查主题标题、普通正文的计算字号，分别记录已安装面板和独立组件页，不把源码声明或截图像素当计算样式。长标题在时间右侧换行。
2. 旧字符串知识可读取、新数组逐条显示、合并去重；真实模型两栏目均返回条目。
3. 选取标题、加粗标签及多条列表进入 Note，完成后再编辑摘录、刷新重新读取；Markdown 导出保留结构，模型收到修改后纯文本并知道其性质。
4. 清空摘录不回填原字幕；修改摘录不改原逐字稿。模拟保存失败保留输入，取消保存后退出，完成结束草稿状态。
5. 过滤脚本和按钮结构，渲染不执行任意 HTML；现有旧笔记正常编辑。最终 YouTube 面板用户验收单独记录。


## 0.2.4 回归项目

检查已手动编辑状态只出现一次且在右侧；两个字段图标与括号选填；旧问答章节能识别，普通句子不变；回答要求可本次修改、保存默认及恢复内置，读取失败有边界；设定入模型请求且不改写旧答案；窄宽度和最终阅读舒适度由用户继续验收。

## 0.3.0 历史记录回归

- 旧记录无更新时间、含异常记录：可读项仍列出，日期不伪造，异常数据不删除。
- 搜索英文标题（不区分大小写）、中文摘录、理解、疑问；已替换摘录旧文和 AI 回答不应命中；点击命中项切换视频并定位笔记。
- 不带 tab 参数打开历史；原视频关闭或播放上下文切换不应清空记录；迟到读取不得回填已卸载页面。
- 草稿编辑时切换视频先保存；模拟失败后仍保留输入；恢复保存可切换；取消保留草稿，完成结束草稿。
- AI 提问需要明确发送，问题和回答均保存；仅翻阅目录、切换语言和空记录不发模型／字幕请求。
- 非视频页面扩展图标打开历史；主面板历史入口替代独立阅读；时间按钮复用原视频或新建对应时间地址。
- 280px 列表和独立滚动；400px 窄屏列表浮层可收起；字重、行高通过浏览器计算样式核对。
- 隔离交互夹具：`pnpm exec vite --config tests/browser/vite.config.ts --host 127.0.0.1 --port 5185 --strictPort`，访问 `/tests/browser/history.html?history=1`。数据仅内存模拟，刷新重置，不接真实密钥或模型。
