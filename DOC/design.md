# YouTube 学习笔记插件设计规范

> 状态：设计方案，尚未实现，也未经过真实页面或用户验收。
> 本文与 [实施计划](PLAN.md) 配套。用户已确认的行为具有优先级；配色、尺寸、布局、图标造型和栏目名称均为本次补齐的设计默认，可根据原型与人工测试调整。
> 第一版以当前电脑的 Chrome 和普通 YouTube 视频页为验证基线；不把下面的像素值或效果描述当成已完成的结果。

## 一、使用目标与边界

让用户始终看清三件事：视频正在讲什么、自己在读哪里、刚记下的内容是否已经保存。视频与学习内容同时可见，常用动作不超过一次明确点击或一个快捷键。

- 第一版支持普通 `https://www.youtube.com/watch?v=...` 视频页。用户已确认 Shorts、正在直播及其他网站嵌入的播放器留到后续，页面不显示入口、面板或快捷笔记窗。电视版等其他页面不宣称支持；直播回放与普通视频全屏另按能力实测，不误写为用户已排除。
- 其他网站不注入入口、面板、浮层或快捷笔记窗口。YouTube 首页、搜索等非视频页面也不显示页面内插件界面。
- Chrome 自己管理的扩展工具栏图标不属于本插件可以按页面强制隐藏的范围，不将“其他网页不显示”误写成隐藏该图标。
- 页面在不同视频之间切换时，视频标题、逐字稿、AI 内容和笔记按视频标识切换，旧视频内容不能短暂显示成新视频的内容。
- 面板关闭只表示收起阅读界面。在支持的视频页，快捷键依然可以创建笔记；此过程不自动打开完整面板。

第一版 AI 区域注明“基于逐字稿”；不提供画面分析、截图分析或视频模型配置入口。讲解文字缺少操作细节时显示简短的来源不足说明，不以猜测补齐教程。

## 二、视觉风格

采用安静的阅读工具风格：中性背景、清晰正文、少量靛蓝强调。避免大面积渐变、玻璃背景、发光描边和宣传式空白。保留 YouTube 原有播放器和页面层级，插件通过统一边框、字重与间距形成一个完整阅读区域。

- 正文阅读优先，时间戳和工具保持次要层级。
- 字幕采用宽松行距；金句和方法卡片使用紧凑的轻边框，不堆叠厚阴影。
- 活动状态同时使用颜色、竖线或文字提示，不单靠颜色表达。
- 双语直接上下排列，不常驻显示“英文原文”“中文翻译”。英文在上、中文在下，通过字重和间距区分。
- 理解与疑问使用线框 SVG 图标，无常驻栏目文字。悬停和键盘聚焦显示中文提示，辅助技术能够读取名称。

### 2.1 明暗主题颜色

默认跟随 YouTube 当前明暗主题；无法识别时跟随系统。设置中允许手动选择“跟随页面／浅色／深色”。下表是实施起点，最终须实际检查文字对比度。

| 标记 | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| `--yn-bg` | `#F6F7FB` | `#14161D` | 面板底色 |
| `--yn-surface` | `#FFFFFF` | `#1B1E27` | 卡片、输入框、弹窗 |
| `--yn-surface-hover` | `#EEF0F6` | `#252936` | 鼠标悬停 |
| `--yn-text` | `#20232B` | `#F2F4FA` | 主正文 |
| `--yn-text-secondary` | `#545B6C` | `#B8C0D2` | 时间、辅助说明 |
| `--yn-text-muted` | `#697184` | `#9BA6BC` | 次要说明，仍需可读 |
| `--yn-border` | `#DCE1EC` | `#353D50` | 装饰性分隔线 |
| `--yn-control-border` | `#7D879B` | `#68748F` | 需要辨认边界的输入框 |
| `--yn-accent` | `#4658CB` | `#A4B0FF` | 选中、链接、焦点 |
| `--yn-accent-bg` | `#EDF0FF` | `#292F4D` | 当前字幕背景 |
| `--yn-accent-on` | `#FFFFFF` | `#161D45` | 实心强调按钮文字 |
| `--yn-warning` | `#875200` | `#F2C36B` | 来源变化、待核对 |
| `--yn-warning-bg` | `#FFF5DF` | `#362C1A` | 温和提示底色 |
| `--yn-error` | `#B12B3A` | `#FF9AA7` | 保存或处理失败 |
| `--yn-success` | `#176A4B` | `#82D6B2` | 已保存、已完成 |
| `--yn-focus` | `#344ED0` | `#BAC4FF` | 2px 焦点轮廓 |

禁用状态优先使用不透明的低强调配色，不能把错误或保存状态一并淡化。文本不直接使用 YouTube 页面继承色，以免宿主主题改变后失去对比。

### 2.2 设计样式：字段与排版参数（当前 0.2.5）

本节是逐字稿、视频脉络及笔记文字样式的集中查找入口，优先于本文后方历史版本记录。2026-09-21：用户已认可 0.2.2 脉络视觉，当前先保留；笔记已按用户确认参数统一为 0.2.3。所有数值为 CSS px，字重 400 为普通、600 为半粗、700 为加粗。表内行高保留实际精度；不将截图物理像素当作 CSS 字号。

#### 字体定义与修改入口

| 代号 | 字体栈 | 使用范围 |
| --- | --- | --- |
| S：系统字体 | `-apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', sans-serif` | 下表所有字段 |
| M：历史等宽字体 | `ui-monospace, monospace` | 0.2.2 笔记时间曾使用；0.2.3 已统一为 S |

Chrome 实际计算字体栈中的 BlinkMacSystemFont 显示为 `system-ui`。这是候选字体列表，不等于每个中英文字符都由同一种具体字库绘制；操作系统选择可用字库。本轮没有把候选字体名称误报为实际逐字形使用的字体。

样式实现入口为 [style.css](../extension/src/ui/style.css)，字段结构入口为 [ContentViews.tsx](../extension/src/ui/ContentViews.tsx)、[AnalysisDetails.tsx](../extension/src/ui/AnalysisDetails.tsx)、[NoteCard.tsx](../extension/src/ui/NoteCard.tsx) 和 [NoteEditor.tsx](../extension/src/ui/NoteEditor.tsx)。后续可以先在本节修改目标参数并标记“待实现”，再同步代码及实测结果；文档不是插件运行时配置，单独修改文档不会改变界面。

#### 已确认：逐字稿

| 字段 | 字体 | 字号 | 字重 | 行高 |
| --- | --- | ---: | ---: | ---: |
| 英文原文（中英／单语） | S | 12 | 400 | 20 |
| 中文翻译（中英／单语） | S | 12 | 400 | 20 |
| 每段时间 | S，等宽数字 | 12 | 400 | 20.04 |

原文、译文统一由 `.segment p[data-language]` 管理；时间由 `.time` 管理。英文仍保留原有次要颜色；用户本轮未要求变更颜色。

#### 已确认：视频脉络

| 字段 | 字体 | 字号 | 字重 | 行高 |
| --- | --- | ---: | ---: | ---: |
| 全片总结标题 | S | 16 | 600 | 26.72 |
| 全片总结内容 | S | 11.25 | 400 | 18.7875 |
| 全片切片判断标题 | S | 16 | 600 | 26.72 |
| 全片切片判断内容 | S | 11.25 | 400 | 18.7875 |
| “基于逐字稿…”辅助说明 | S | 12 | 400 | 20.04 |
| 每个片段小标题 | S | 15 | 600 | 22.5 |
| 片段时间范围 | S，等宽数字 | 12 | 400 | 20.04 |
| 片段概括正文 | S | 11.25 | 400 | 18.7875 |
| 解决问题／适用场景／切片建议标签 | S | 11.25 | 700 | 18.7875 |
| 上述三项分条内容 | S | 11.25 | 400 | 18.7875 |
| 知识清单总标题 | S | 16 | 600 | 26.72 |
| 每个知识点标题 | S | 15 | 600 | 22.5 |
| 需要理解／视频中的作用标签 | S | 11.25 | 700 | 18.7875 |
| 需要理解／视频中的作用分条内容 | S | 11.25 | 400 | 18.7875 |
| 视频依据文字 | S | 12 | 400 | 20.04 |
| 视频依据时间范围 | S | 12 | 400 | 20.04 |
| “另 × 处”折叠提示 | S | 12 | 400 | 20.04 |
| 前置知识折叠入口 | S | 11.25 | 400 | 18.7875 |
| 前置知识名称（RPA／MVP 等） | S | 15 | 700 | 25.05 |
| 前置知识解释正文 | S | 11.25 | 400 | 18.7875 |
| 前置知识来源：讲者明确／AI 延伸 | S | 12 | 700 | 20.04 |
| 金句总标题 | S | 16 | 600 | 26.72 |
| 金句中文／英文内容 | S | 11.25 | 400 | 18.7875 |
| 金句分类、时间及核实提示 | S | 12 | 400 | 20.04 |
| 有效方法总标题 | S | 16 | 600 | 26.72 |
| 每个方法小标题 | S | 15 | 600 | 22.5 |
| 方法说明正文 | S | 11.25 | 400 | 18.7875 |

五个大标题统一使用 `.analysis-section-title`，前面添加 4 × 18px 主题色竖条，圆角 2px，距文字 8px；颜色来自 `--accent`，浅色模式为 `#4658cb`。空内容伪元素不混入复制文字。三类小标题统一使用 `.analysis-item-title`。前置知识名称保持原样，不混入三类小标题的新规则。

核查依据：0.2.2 已安装 Chrome 独立阅读页实测五个大标题及 35 个小标题；0.2.2 内嵌面板及直接使用的 CSS 规则核对其余字段。此前 0.2.1 内嵌面板完整实测用于核对本次未变动的字段；不是用历史设计默认值推断当前字号。

#### 已确认：笔记展示卡片（0.2.3）

所有字段统一使用 S 系统字体，时间仅保留等宽数字排列，不再使用单独等宽字体家族。

| 字段 | 字体 | 字号 | 字重 | 行高 |
| --- | --- | ---: | ---: | ---: |
| 笔记／草稿标识 | S | 14 | 600 | 21.71 |
| 时间 | S，等宽数字 | 14 | 600 | 21.71 |
| 摘录正文、段落与列表（包括旧笔记） | S | 13 | 400 | 20.72 |
| 摘录内部小标题 | S | 13 | 600 | 20.72 |
| 我的理解与价值、个人疑问正文 | S | 12 | 400 | 20.72 |
| AI提问折叠标题 | S | 11.25 | 400 | 18.79 |
| AI回答段落与列表 | S | 13 | 400 | 20.72 |
| 已手动编辑／AI回答需核对提示 | S | 12 | 400 | 20.04 |
| AI提问／编辑／跳转／删除按钮 | S | 11.25 | 400 | 18.79 |
| 非划词笔记的英文引用／中文译文 | S | 13 | 400 | 20.72 |

顶部按“书本图标、笔记／草稿、链接图标、时间”顺序同排居左；图标为细线 SVG（书本 16px，链接 14px），标识与时间之间间距 8px。时间仍点击跳转，图标为装饰，不增加新的操作。“AI 梳理摘录”文字不再显示，内部来源信息保留。原有笔记标识字距恢复默认。

#### 已确认：笔记编辑表单（0.2.3）

| 字段 | 字体 | 字号 | 字重 | 行高 |
| --- | --- | ---: | ---: | ---: |
| 顶部时间／编辑笔记 | S | 13 | 600 | 20.72 |
| 顶部关闭按钮 | S | 11.25 | 400 | 18.7875 |
| 摘录标签 | S | 12 | 400 | 18.79 |
| 可直接编辑说明 | S | 12 | 400 | 20.04 |
| 摘录输入正文／列表 | S | 13 | 400 | 20.72 |
| 摘录内部小标题 | S | 13 | 600 | 20.72 |
| 加粗／列表／小标题／正文工具按钮 | S | 11.25 | 400 | 23 |
| 我的理解与价值／我的疑问字段标签 | S | 13 | 600 | 20.72 |
| 选填提示 | S | 12 | 400 | 20.04 |
| 两项个人输入框的文字 | S | 13 | 400 | 20.72 |
| 保存状态／手动编辑说明 | S | 12 | 400 | 20.04 |
| 删除／取消／AI提问／完成按钮 | S | 11.25 | 400 | 18.79 |
| AI提问折叠标题 | S | 11.25 | 400 | 18.79 |
| AI回答段落／列表 | S | 13 | 400 | 20.72 |
| 非划词笔记的英文／中文摘录输入框 | S | 13 | 400 | 20.72 |

四个底部按钮同排，完成紧邻 AI提问，最小高度 32px、内边距上下 4px／左右 10px，与卡片操作按钮尺寸规则一致。卡片和编辑表单的 AI 问答初次呈现均默认折叠，点击展开，长问题自然换行。展开中的用户手动加粗内容保留，不强行取消强调。

#### 摘录来源与一致性规则

旧 `excerptFontSize` 数据保留以兼容记录，但卡片与编辑器不再读取它作为显示字号；旧笔记、逐字稿来源及脉络来源统一为上述 13px。保留标题、列表、段落与手动加粗结构；加粗文字仍可为 700。并未修改来源逐字稿或用户笔记内容。

卡片个人内容 12px、输入框内容 13px 是用户确认的阅读／编辑层级差异；AI 问答与摘录在两处保持同一组参数。辅助提示保持 12px／400／20.04px，明确设置行高，避免继承字段标签后变化。

### 2.3 间距、形状与图标

- 间距基线：4、8、12、16、20、24、32px。面板左右内边距 16px，卡片内部 12px，卡片之间 12px。
- 面板圆角 14px，卡片 10px，按钮和输入框 8px；分割线 1px。
- 正常图标 18×18px，入口图标 16×16px。统一 `viewBox="0 0 24 24"`、`stroke="currentColor"`、1.75px 线宽和圆形端点。
- 图标按钮可见尺寸不必很大，但点击区域至少 32×32px，主要入口至少 36px 高。
- 面板仅使用轻阴影；浮窗阴影建议 `0 12px 36px rgba(0,0,0,.16)`，深色对应提高到 `.32`。
- 理解默认使用线框灯泡，疑问默认使用带问号的对话框；两者可由轮廓区分，不依赖黄色或其他颜色。
- 阴影、图标形状和以上色值为设计默认，尚无原型偏好证据。

## 三、页面入口与布局

### 3.1 视频下方的小入口

入口文字暂定“学习笔记”，左侧放简洁的文档播放图标。默认高度 36px，左右内边距 12px，图标与文字间隔 6px，圆角 18px，宽度由文字决定，约 100–116px。

- 优先在播放器下方、视频信息与操作区域旁的可用位置增加独立入口，不能覆盖点赞、分享、订阅或视频标题。
- “空缺处”不是假定永远存在的固定屏幕坐标。原操作行没有空间时，在其下方插入插件自己的窄入口行，与原内容间距 8px。
- 不移动或删除 YouTube 原有操作。入口只挂载一次，视频切换后继续保持唯一。
- 点击打开面板，再次点击收起。图标和 `aria-expanded` 同步变化，入口保持可见，不通过展开整个面板来关闭。
- 面板隐藏后仍可用快捷键摘录。首次创建成功时使用一次简短提示“笔记已保存，可在学习笔记中查看”，不反复教育用户。

### 3.2 宽屏默认布局

默认采用页面内面板，位于播放器右侧推荐区域；面板打开时，让推荐内容顺延到面板之后，关闭时恢复页面布局。不能用悬浮层盖住播放器、字幕或原操作区。

- 视口宽度至少 1280px 且实际可用右侧空间至少 380px 时启用右侧布局。
- 默认宽度 440px，可拖动调整为 380–560px；不能让面板把播放器压缩至不可阅读，应按实际空间切换布局。
- 面板顶部随页面布局定位，滚动后可吸附在导航栏下方，预留 12px 间距。
- 面板最大高度为视口高度减去导航栏、顶部与底部留白；内部内容独立滚动。
- 顶部标题、标签页、语言与搜索工具、底部跟随区域保持固定，只有内容区滚动。

```text
┌──────────────────── YouTube 导航 ────────────────────┐
│                                                       │
│ ┌─────────────────────────┐ ┌───────────────────────┐ │
│ │                         │ │ 学习笔记      设置  × │ │
│ │        视频播放器       │ │ 当前视频标题（两行内）│ │
│ │                         │ ├───────────────────────┤ │
│ └─────────────────────────┘ │ 逐字稿  AI 梳理  笔记 │ │
│ 视频标题／原有视频操作        │ 原文 中文 中英双语    │ │
│ [学习笔记]                  │ 搜索逐字稿……          │ │
│ 视频说明                    ├───────────────────────┤ │
│                             │ 02:15                 │ │
│                             │ English sentence...   │ │
│                             │ 对应中文……            │ │
│                             │                       │ │
│                             │ 高亮当前播放段落      │ │
│                             ├───────────────────────┤ │
│                             │ 自由阅读      [跟随]  │ │
│                             └───────────────────────┘ │
└───────────────────────────────────────────────────────┘
```

### 3.3 窄屏、剧场模式与全屏

| 场景 | 布局默认 |
| --- | --- |
| 宽度 960–1279px，或右侧不足 380px | 将面板放到播放器下方，与视频信息区保持文档流布局；默认内容高 520px，不遮播放器 |
| 宽度低于 960px | 下方面板占可用宽度；左右内边距 12px；内容高取 420px 与可用视口高度的较小值，低高度场景改为自然高度 |
| 窗口很矮 | 优先保留输入与必要操作，减少固定区域，允许面板随页面滚动，不形成只能显示一行正文的内容窗 |
| YouTube 剧场模式 | 面板切到播放器下方，不占剧场播放器的画面位置 |
| 全屏播放 | 页面入口自然不可见；不强制退出全屏。全屏内快捷笔记浮层是否能可靠呈现列为能力测试项，失败时先保存草稿并在退出全屏后提示，不承诺已支持全屏浮层 |

拖动宽度设置只在当前电脑保存；离开视频页撤下所有插件页面 UI。浏览器放大时按可用 CSS 空间重新选择布局。

## 四、逐字稿界面

### 4.1 结构与正文

- 标签页下方显示“原文／中文／中英双语”分段控件，每项高 32px，当前模式使用底色与选中状态。
- 第一版初始模式暂定中英双语，之后记住用户选择；这是设计默认，用户未亲自指定初次默认值。
- 搜索框高 36px，支持当前可用原文或译文关键词，显示命中数量和上一条／下一条；未生成的译文不伪称已搜索。
- 每段时间放在上方，占 18px 行高，距离正文 4px。双语之间 6px；段落容器上下 12px、左右 12px，避免时间列挤压正文。
- 当前播放段使用 3px 强调色左竖线与浅色背景，保留至少 9px 文字内边距；高亮不改变字号、行高或容器尺寸。
- 鼠标悬停显示编辑图标；键盘聚焦时同样可用。用户不悬停时，工具不反复占据正文。
- 只显示所选模式需要的正文。AI 梳理始终默认中文，不随模式变化重新生成。

### 4.2 跟随与跳转规则

- 跟随模式使当前段位于内容可见区约上方三分之一处，留出后续内容；当前段已处于合理可见范围时不做小幅抖动滚动。
- 用户滚轮、触控板、拖动滚动条或键盘主动浏览后进入自由阅读。程序引起的定位滚动不能误判为用户操作。
- 自由阅读时继续计算当前高亮，只停止自动滚动；底部固定栏高 48px，显示“自由阅读”和主按钮“跟随”。
- 已跟随时显示“正在跟随”，按钮仍可用于重新定位；不要仅通过禁用按钮传达当前状态。
- 点击正文或时间戳跳转到该段开始并播放，默认恢复跟随。若浏览器拒绝播放，位置保留并显示“已定位，点击视频继续播放”，不能显示播放成功。
- 拖动选词、已有非空选区、点编辑图标、输入编辑、点击选词工具条均不得触发视频跳转。
- 空白时段保留视频当前位置提示，不错误高亮未来段；广告期间暂停正片跟随并提示“广告播放中”，不将广告时钟用作正片时间。

### 4.3 加载、空状态与失败

| 状态 | 可见内容与动作 |
| --- | --- |
| 首次打开、尚未处理 | 当前视频标题、真实阶段提示和进度条；工具不显示可误操作的假字幕 |
| 正在获取已有字幕 | 不确定进度条与“正在获取字幕”，无虚构百分比 |
| 无字幕，等待生成 | “未找到可用字幕，正在生成逐字稿”；如果需确认费用则先停在明确确认状态，不假称已开始 |
| 正在翻译 | 能计算已完成段数时显示真实进度；按需显示已完成内容并标明“翻译中”，不将未完成区当成全部结束 |
| 只有原文可用 | 原文可读；中文／双语模式明确显示译文待处理或失败，允许重试 |
| 获取失败或额度不足 | 中文说明、重试或查看设置入口；已存在的笔记与缓存仍可访问 |
| 视频切换 | 立即显示新视频标题与新视频加载状态，旧稿退出当前视图；异步旧请求不得回填 |
| 视频身份未确定 | “正在识别视频”，不显示上一个视频标题，也不允许把新笔记错误绑定到上一个视频 |

进度条高度 4px、圆角 2px，状态文字 13px；详细说明可展开，默认不向用户展示接口字段、堆栈或服务内部编号。

## 五、笔记界面与快捷记录

### 5.1 笔记列表与已保存内容

- 默认只显示当前视频的笔记，按时间点升序；同一时间按创建顺序。标题处显示笔记数。
- 每张卡片包含起止时间、双语引用、灯泡输入内容、问号输入内容、保存状态和编辑入口。
- 中英文直接排列，不添加“英文原文”“中文翻译”标签。
- 用户输入区用 18px 灯泡／问号 SVG 标识；无常驻“我的理解／我的疑问”文字。输入框允许简短占位提示“写下想法…”和“留下疑问…”，填写后提示消失。
- 已保存笔记仍可编辑原文引用、译文引用、理解和疑问。修改引用只改变这条笔记的引用，不反写逐字稿。
- 编辑态提供“完成”与“取消本次修改”；输入过程中保存草稿，完成后持久保存并回到阅读态。保存失败保留输入并显示重试，不显示“已保存”。
- 自动保存状态在实际本地写入成功后显示；不弹出打断式成功对话框。
- 切换标签或收起面板不清空编辑草稿。多个笔记编辑草稿按笔记标识分别保存，不相互覆盖。
- 删除不是本轮新增核心需求；若后续增加，必须独立设计可撤销与确认流程，不把编辑入口兼作删除。

### 5.2 轻量快捷笔记窗口

快捷笔记窗口是页面内非模态对话框。默认位于视口右下方，边距 20px，宽 400px，最大高为视口高减 40px；窄屏宽度取 `calc(100vw - 24px)`，边距 12px。

```text
┌─────────────────────────────────┐
│ 记笔记                      ×  │
│ 当前摘录视频标题               │
│ 08:42–09:06                    │
│ English sentence...            │
│ 对应中文……                    │
│                                │
│ [灯泡] 写下想法…               │
│                                │
│ [问号] 留下疑问…               │
│                                │
│ 草稿已保存           [完成]    │
└─────────────────────────────────┘
```

- 在支持的视频页，面板从未打开、已关闭或已打开时，快捷键行为一致。
- 按键瞬间冻结视频标识、标题和正片时间；视频随后继续播放不改变这次摘录的时间。
- 默认不暂停视频，也不重排主面板；这是减少观看中断的设计默认。用户可直接操作原播放器暂停。
- 有当前视频字幕缓存时立即摘录对应段；没有缓存时先创建待补全草稿，显示“正在补全此时刻的双语摘录”，允许立即输入想法或疑问。
- 只补全当前笔记的引用，不自动生成整段 AI 摘要；字幕供应商仍可能需要获取或处理全片，额度提示按实际请求计算。引用待补全失败时保留用户输入、视频链接和冻结时间，提供重试。
- “完成”保存后关闭窗口；关闭按钮与 `Escape` 收起窗口并保留草稿，不把关闭等同于删除。
- 自动把焦点移到想法输入框；`Tab` 可移到疑问与完成按钮。非模态窗口不锁定整个页面焦点，视频控制仍可操作。
- 已有未完成窗口时再次按快捷键，默认聚焦现有窗口并提示“先完成这条笔记”，不悄悄替换摘录；该行为作为防丢输入的设计默认。
- 切换到另一个 YouTube 视频时收起旧视频窗口并保存其草稿。新视频可创建新笔记，旧草稿不改成新视频来源；返回原视频后可恢复。
- 离开支持视频页时不显示窗口，但草稿保存在原视频下。
- 快捷键实际组合由技术方案与冲突测试确定；界面读取实际配置展示，不把示例按键硬写到提示。快捷键范围为当前支持的 Chrome 视频标签页，不承诺在其他应用全局截取。

### 5.3 划词摘录

- 鼠标选中非空逐字稿文字后，在选区上方或下方 8px 处显示 32px 高“做笔记”工具条，不盖住已选文字、不超出视口。
- 点击工具条打开同样的轻量笔记窗口，保持选区来源和时间关联。
- 默认保留用户精确选中文字，并附所属完整段的对应另一语言；原文与译文的局部词组不承诺一一对应。该粒度仍属于待样本验证的设计默认，不能伪造精确对齐。
- 跨多个段落时保留段落边界与各自来源时间，不把不连续片段编造成一句原话。
- 选择区域改变、用户清除选区、视频切换、进入编辑态或离开面板后，工具条收起。
- 只在本插件逐字稿选区显示入口，不劫持 YouTube 页面其他文字选择。

### 5.4 来源修改与用户修订

- 笔记引用保存摘录时快照。来源修改后显示轻量提示“来源有更新”与“查看差异”，不自动替换引用。
- 差异视图以“当前引用／来源新版”为短标签，逐行或逐段展示变动；新增与删除同时使用符号和颜色。
- 窄面板默认上下对照；宽度允许时再并排。理解和疑问仍在原卡片保留，不进入引用替换范围。
- 操作明确为“更新引用”和“保留原样”。保留后不重复提示同一个来源版本；来源再次变化可再次提醒。
- 若用户还修改过笔记引用，额外提示“这条引用有你的修改”；更新前清楚展示替换范围，不静默覆盖手动修订。
- 原文修改后默认保留现有译文并标“待核对”，手动重译单独触发；这是保守设计默认，须和技术方案、用户后续判断保持一致。

## 六、AI 梳理界面

顶部标签暂称“AI 梳理”，内部顺序为“概览、视频脉络、金句、方法”。“视频脉络”仍为候选栏目名，不显示“第一章、第二章”。

- 顶部概览为 2～3 句、约 100 字的全片总结。其后显示全片切片粗判断，汇总各段内容独立性，不预测流量。
- 视频脉络卡片的首行是时间范围，随后是主题标题；正文分别回答内容、问题、应用场景与切片建议。
- 卡片默认展开内容介绍，其余内容可展开；全部展开和折叠提供统一入口，用户不必连续点几十次。
- 按 2026-09-21 最新确认，“切片建议”第一版只给全片总判断和每段粗判断（建议切片／有条件建议／不建议单独切片）与价值、上下文条件。主题时间用于回看，不冒充精确切片范围；详细切片规划后续再做。不提供“剪辑”“生成视频”或“下载片段”按钮。第一版固定依据文字，紧邻显示“基于逐字稿”，不宣称已看过画面。
- 金句按反直觉洞察、惊人事实、轶事、点透本质筛选；双语引用配可点击的来源时间，不设“跳转原文”按钮。淡色双页图标默认复制双语，旁边小箭头可选中文或原文；有复制成功／失败反馈。惊人事实保留讲者陈述性质，不表示已核实。方法卡片使用中文名称与说明。应用场景用 2～3 句交代具体处境与用法，来源标注“讲者明确”或“AI 延伸”。
- 视频脉络之后增加知识清单，逐项展示需要理解什么、视频中的作用和可点击时间；前置知识在独立折叠小块中说明必要基础、程度与用途，无需基础不凑数。
- 时间入口跳转到对应视频位置并播放；“加入笔记”保存对应引用和来源，不自动填入用户的理解与疑问。
- 生成由用户明确操作触发；面板打开、语言切换和快捷摘录均不隐式重生成已有 AI 内容。
- 空状态使用简短说明与“生成梳理”按钮。生成、失败、部分完成、缓存可用分别显示，不让失败遮掉已保存结果。

## 七、编辑、设置和导出

- 逐字稿编辑采用当前段内联编辑；原文与译文分别可编辑，双语模式允许同时核对。完成／取消按钮只在编辑态显示，编辑时正文不响应跳转。
- 中文手动修正立即成为用户保存的版本，不自动触发重新翻译，也不能被在途旧翻译覆盖。
- 设置为专门的完整网页，在独立 Chrome 标签页打开；面板齿轮与 Chrome 扩展选项均进入同一页面，离开 YouTube 后也可以主动打开。
- 设置栏目包括常规、LLM 服务、字幕与外部 API、任务与模型、系统钥匙串。密钥输入只放在独立扩展设置页，不在 YouTube 页面 DOM 内收集。
- 系统钥匙串栏目展示“组件未安装／组件可用／版本不兼容／等待系统授权／钥匙串锁定／授权被拒绝／启动失败”。提供安装说明、检测和重试；组件空闲未运行不报离线。API key 保存后清空输入，仅展示已配置状态，不提供配对码或插件解锁密码；系统密码只在 macOS 系统窗口输入。已有缓存和笔记仍可用。首次安装、授权与升级体验须真实验证。
- 导出窗口分别选择“逐字稿／笔记／AI 梳理”，默认不一次全部勾选；逐字稿可选原文／中文／双语与时间戳。
- 笔记默认 Markdown，逐字稿默认 TXT 或 Markdown，是本版设计建议。笔记导出保留双语、来源、时间、理解和疑问；待补全或失败内容明确标记，不隐瞒缺项。
- 用户导出完整中文或双语逐字稿而仍有缺失时，展示缺失情况，并允许先补齐再导出；不能把部分文件称为完整。
- Obsidian 直接写入不出现在首版可点击操作中。未来能力可写入文档，避免用无效按钮占据主界面。

### 7.1 独立设置页布局

整体最大宽 1120px、左右留白 32px，左导航宽 200px，与内容间隔 32px；单个表单最大宽 640px。低于 800px 时导航移到上方可换行，表单占可用宽度，左右留白 16px。沿用现有明暗 token，不单独引入另一种视觉风格。

- 页标题 24px／32px 行高、字重 600；分区标题 18px／28px；表单标签 14px／20px；帮助及错误说明 13px／20px。
- 输入控件高 40px，组间距 20px，标签与控件间距 6px；连接结果卡片内边距 16px。
- 页底提供可见的保存区域；有未保存更改时提示，不能把表单中输入的值自动当成已启用配置。
- 设置属于需要明确辨认的表单，保留“API key、模型、接口地址”等标签；笔记里的无常驻标签要求不延伸到这里。

```text
┌──────────────────────────── 设置 ──────────────────────────┐
│ 常规             │ LLM 服务                              │
│ LLM 服务         │ [日常连接 ▼]              [添加连接]   │
│ 字幕与外部 API   │ 连接名称   [                          ]│
│ 任务与模型       │ 接口地址   [                          ]│
│ 系统钥匙串       │ API key    [输入新密钥以替换          ]│
│                  │            已配置 · 系统凭据库        │
│                  │ 默认模型   [选择或输入模型 ID         ]│
│                  │ [获取模型]  [测试连接]                 │
│                  │ 最近测试：尚未测试                     │
│                  │ 高级设置 ›                             │
│                  │                           [保存配置]   │
└───────────────────────────────────────────────────────────┘
```

### 7.2 LLM 与外部服务交互

- LLM 服务支持连接名称、实际支持的协议、Base URL、掩码密钥输入和默认模型，模型列表不可用时保留手动输入。
- “任务与模型”分别选择翻译模型和 AI 梳理模型，默认共用；Supadata 独立选择为字幕来源，不要求用户填 LLM 模型来获取已有字幕。
- Supadata 卡片提供密钥、字幕模式、允许生成与限制及测试状态；其他尚未适配服务不放无效表单。
- 测试状态显示未测试、测试中、模型可用或明确失败原因及时间。网络通但模型不可用时不能显示全部成功。
- 测试使用当前表单，但不自动保存；保存也不等于测试通过。测试按钮说明可能产生少量调用费用，不默认上传私人视频内容或笔记。
- 已保存密钥仅显示状态，不回填或提供查看完整 key；空输入表示不替换。保存成功后清空新输入，失败保留旧有效连接。环境变量来源显示只读来源说明。
- 切换模型不自动重生成逐字稿和摘要；当前运行任务与结果保持其原配置关联。
- 页面表单细则、凭据保存和服务能力边界以 [service-settings.md](service-settings.md) 为准。

## 八、动效、键盘与无障碍

- 按钮状态变化 120ms，面板展开和轻窗出现 160ms；不使用弹跳和长滑入。
- 跟随滚动上限约 180ms，快速拖动视频时立即定位，不能排队播放旧滚动动画。
- 尊重 `prefers-reduced-motion`：关闭非必要过渡、改为直接滚动；加载状态仍以文字说明。
- Tab 按逻辑顺序访问入口、标签页、语言模式、工具、段落操作与底栏。选中段落的跳转可通过明确按钮或键盘触发，不要求用户只使用鼠标正文点击。
- 标签页使用标准选中语义和方向键切换；弹窗有可读名称。仅真正阻止背景交互的差异确认对话框使用模态语义与焦点约束。
- 理解图标辅助名称为“理解或想法”，疑问图标为“疑问”；编辑、关闭、导出图标各有对应中文名称。辅助名称不等于常驻视觉标签。
- 保存、失败和视频切换使用温和状态播报；每次播放段高亮不反复向屏幕阅读器播报，以免盖住视频声音。
- 焦点轮廓 2px、偏移 2px，不能被卡片圆角截断。关闭窗口后焦点回到触发入口或当前视频的合理位置。
- 正文目标对比度至少 4.5:1，必要非文字控件和焦点至少 3:1；必须实际测量，不能仅凭本配色表宣称已满足。

## 九、设计验收与待验证项

以下均是待执行标准，测试结果记录到测试反馈文档，不在本文标为通过。

| 检查项 | 验收方式与目标 |
| --- | --- |
| 尺寸与布局 | 以 1440×900、1280×800、1024×768、800×700、1280×600 CSS 视口检查；播放器无覆盖、正文无横向滚动、按钮可达 |
| 字体与缩放 | 100%、125%、200% 浏览器缩放后文字不裁切，笔记输入和完成按钮可操作 |
| 主题 | 明暗主题各检查正文、选中、禁用、失败、焦点与占位提示；对比度不通过则修正 token |
| 页面入口 | 视频切换和返回后只有一个入口；无空间时换行；YouTube 非视频页和其他网站无页面 UI |
| 逐字稿行为 | 当前高亮不导致列表抖动；自由浏览不被拉回；跟随与段落跳转可区分；选词和编辑不误跳 |
| 视频切换 | A→B→A、快速连续切换和浏览器前进后退均不串稿、串笔记；旧请求不能闪现为新视频结果 |
| 关闭面板记笔记 | 从未打开与主动关闭两种状态都能触发快捷摘录；浮窗不强迫打开主面板，不清空用户输入 |
| 待补全与离开页面 | 无缓存、翻译失败、切换视频时，草稿仍保留原视频与时间；无字幕不妨碍先写个人想法 |
| 笔记编辑 | 保存后可修改；失败保留输入；来源更新前后个人理解和疑问逐字保持不变 |
| 极端内容 | 长标题、超长英文词、长段中文、跨段选区和大量笔记不撑破布局；截断标题可查看全文 |
| 动效与辅助 | 减少动效、纯键盘、可读图标名称、焦点恢复和真实进度均实际验证 |
| 人工体验 | 用户确认入口位置、双语阅读密度、SVG 含义、快捷记录过程与窄屏布局；未确认前仅称设计默认 |

仍需实际测试的假设包括：右侧面板宽度是否适合用户当前屏幕、YouTube 页面结构变化后的入口定位、全屏内快捷记录能力、精确选词配整段译文是否符合预期，以及无缓存补全的真实等待时长。验证结果改变交互时，同步更新本文、实施计划和测试方案。

## 十、本地翻译与主动大模型翻译

- 普通翻译优先本地，首次准备阶段显示模型下载状态与真实进度；未验证或不支持不能显示可用。
- 每段操作区增加“用大模型翻译”，不与点击正文跳转混淆；全文翻译置于独立菜单，操作前展示处理范围及额度提示。
- LLM 返回后显示候选译文与“应用／保留当前译文”；不自动替换，已有手改与后续修订均受保护。
- 本地翻译失败显示重试和主动 LLM 入口；未配置模型则进入设置。没有用户操作不发云端翻译请求。
- 笔记补齐同样遵守该规则；翻译暂不可用时先保存时间、原文和个人输入，不阻塞记笔记。

## 十一、无字幕生成确认

无字幕时显示视频标题、时长、服务名称、预计积分和估算依据，注明最终以服务计费为准。按钮为“暂不生成”和“确认生成”，不自动选中确认、不提供永久跳过。信息不足时禁用确认并提示无法估算，可重试或继续记笔记。快捷摘录中同样先保存草稿再询问；关闭视为暂不生成，保留输入。已有任务查询或缓存恢复不反复弹窗。

## 设置页调整（2026-09-11，以本节覆盖原设置页视觉默认）

- 设置页采用中性灰底、白色卡片、灰绿强调色；只在设置页作用域覆盖变量，不影响视频面板。浅色强调 #4B655D，浅背景 #EAF0ED；深色使用 #B2CEC1 与 #263C35。
- 主标题 26px，栏目标题 18px，卡片标题 17px，表单正文 14px，字段标题 13px，辅助说明 12px。使用系统字体。
- 左侧宽 190px，右侧最大 720px，间距 40px；导航改为 44px 高按钮，序号、中文名称与选中背景统一展示，不使用浏览器默认蓝色链接。窄屏改为折行导航。
- 输入框最小高度 42px；右侧 SVG 小叉点击范围 32px，内容为空时隐藏，清空后焦点回输入框。可编辑名称、地址、模型、临时密钥和高级费率使用同一控件。Supadata 固定只读地址不放清空按钮。
- 清空临时 API Key 不删除系统钥匙串已有密钥，紧邻输入框说明。切换服务商地址清空旧的临时 Key 与模型列表，原保存密钥不自动发送到新地址。
- 服务商选择与自定义连接名称分开；支持 DeepSeek、智谱、OpenAI，以及自定义兼容接口。输入已知服务商名称后，在离开输入框时匹配地址与默认模型，不在逐字输入过程中反复改写。
- 字幕费用独立栏目：直接已有字幕、外部已有字幕、外部生成分别展示；官方费率为默认参考，高级调整折叠，附官方价格及额度后台入口。
- 本机 Codex 连接为已核实可行、尚未实现的独立接法；不放入 API 服务商列表伪装成可用 API。

## 保存状态与 Codex 试用补充

- 卡片右上角显示配置已保存／未保存，与下方连接测试结果分离。测试成功保留结果；保存全部后更新全部卡片的保存状态。未修改配置时点击测试不会变为未保存。
- 页面顶部另显示整体保存状态，包含模型分工和费率变化；个人连接改回原值且临时 Key 为空时恢复已保存。
- 本机 Codex 接法已实现，独立入口创建连接卡片，不展示 API 地址与 Key。模型可留空使用 CLI 默认，也可手填；检测本机登录和测试模型分别提供反馈。试用连接已经过实际本机组件测试并保存，完整 UI 回归仍以测试反馈为准。

## 模型名称可见性补充

模型分工的 LLM 选项采用“连接名称 · 完整模型名”，连接卡片增加当前模型文本；空值显示“未选择模型”，不得用模糊的默认模型替代。模型名称为普通可见文本，只有 API Key 掩码。Codex 已存在时入口显示“查看本机 Codex”并滚动定位到已有卡片。

## 扩展应用图标

采用用户提供的黄橙到粉色渐变参考：圆角方形底、白色播放三角、不闭合圆环及右上角星光。外部圆角透明，不使用字母占位图标。图标资源放在 `extension/public/icons/`，通过 Vite 复制至构建目录；manifest 的应用图标与工具栏图标共用 16、32、48、128px PNG，页面标签使用 32px 图标。此项仅改变应用标识，不调整设置内容或业务行为。

## 自动准备与加载状态

打开面板后自动获取已有字幕，中文／双语模式自动本地翻译；按钮改为失败重试入口。加载时右侧内容区由完整状态页占据，保留标题及页签：58px 渐变播放标识、16px 阶段标题、12px 辅助文本、8px 渐变圆角进度条和骨架占位。未知总量不伪造百分比；分段翻译及整理显示实际完成比例。LLM 翻译直接更新无冲突的内容，只有手改或版本冲突时显示译文对照与显式替换按钮。

## 0.1.1 面板交互调整

展开态顶部移除大标题，保留小号版本、设置及收起按钮；收起后右栏保留圆角横条，左侧为扩展名称，右侧“展开”。保留原 iframe 以继续任务和恢复阅读位置。视频下方入口与浏览器扩展图标均执行展开。笔记列表和编辑窗口加入删除按钮，逐条确认。独立阅读标题区显示“返回原视频”。移除常驻本地翻译按钮，仅在浏览器明确要求首次用户激活时显示一次“继续准备”入口。

## 0.1.2 排版修正

右侧双栏面板在挂载／窗口调整时对齐视频可视区域顶部；窄屏及影院模式不做向上覆盖。顶部去版本号，操作靠右，内边距改为上 8px、下 12px。加载界面用整体内容容器居中，图标与 8px 进度条禁止收缩，避免顶格或进度条被压没。笔记原文、中文、理解与疑问统一 14px、1.7 行高；图标 18px，与首行文字居中对齐；编辑与删除在卡片右侧成组。简短视频总览目标约 100 字，长总结回退时折叠，不占据首屏。

## 0.1.3 标题与设置图标

取消单独占一行的顶部操作栏，视频标题与右侧操作在同一 flex 行顶部对齐。标题 13px、1.55 行高，剩余宽度自然换行；操作不收缩。设置按钮 32px 方形、18px SVG 居中，齿轮采用围绕中心等间距的八齿轮廓和圆角连接。

## 0.1.4 笔记参考样式

笔记卡片采用 3px 蓝色左边线、12px 圆角、轻阴影与 14px／16px 内边距。顶行左侧笔记／草稿标识，时间右对齐。理解置于引用前，原文使用浅灰引用块与左竖线，中文置于下方，疑问保持图标与首行对齐。正文统一系统字体 14px、1.7 行高，不照搬参考图中不一致字体；底部编辑、跳转和删除按用户既有要求靠右。不添加虚假的收藏状态。

## 0.1.5 操作区与导出

语言用三个并排按钮，选中态 aria-pressed；只保留“LLM翻译”和“独立阅读”。底部左侧导出，右下角圆角跟随按钮。导出以居中弹窗展示三个复选项和四个格式单选项，默认全选内容，逐字稿遵循当前语言模式。至少选一项才允许导出；PDF 明示通过打印窗口保存。全片总结单独标题，各主题介绍位于对应主题卡片，不展示冗长的原先技术说明。


## 0.1.12 界面更新

主题卡片首行为左侧时间、右侧标题，正文三个栏目加粗且用列表。知识来源使用紧凑文本时间，首处显示、其余折叠；知识卡片有 note 和 AI提问。逐字稿与脉络划词后在选区下方显示淡色操作条，滚动时收起。“脉络”操作位于底部跟随左侧，原大按钮和重新分段／补齐本地翻译操作行取消。金句分类按用户再次确认保留反直觉洞察、惊人事实、轶事、点透本质。

问答为独立弹层，先保存问题草稿，回答按 AI 内容展示，可继续提问并在笔记中查看；不将回答填入用户个人理解。模型输入范围和额度提示紧邻提问入口。


## 0.2.0 阅读与笔记精简（2026-09-21）

- 用户确认模块标题降为 16px；主题小标题与正文均 15px，小标题 600 字重，正文普通字重。知识清单“需要理解”“视频中的作用”加粗，段落间距 12px。
- 划词 Note 只保存选中文字及定位信息，不自动补齐关联整段双语逐字稿。知识卡移除常驻 note／AI提问，划词操作保留。
- 表单为摘录、我的理解与价值、我的疑问；后两项选填。删除／取消／AI提问／完成同一行，完成紧邻 AI提问且位于右侧。
- 取消保存最新输入后退出，不恢复编辑前快照；新笔记仍为草稿，已完成笔记保持完成状态。只有完成动作结束草稿状态，删除才移除笔记。保存失败不关闭编辑器。
- 五类金句为反直觉洞察、点透本质、方法与原则、关键事实、案例与经验，不强求各类齐全。读取旧数据时将惊人事实映射为关键事实、轶事映射为案例与经验；新方法类须重新整理后提取。
- 旧划词笔记的存储原文保留，显示、导出及提问不再重复附加。已有问答历史保持；继续追问仍附上最近四轮问答，不自动清理旧回答中的引用。
- 无新增权限或依赖。Chrome 最终视觉与全链路仍待用户验收。


## 0.2.1 实际字号纠正与摘录排版

此前“标题和正文均为 15px”的判断来自独立验证页，不能代表已安装的 Chrome 扩展。2026-09-21 在已安装 0.2.0 的独立 panel.html 页面读取同一视频缓存，计算样式为主题标题 15px／600，正文 11.25px／普通字重／18.7875px 行高；DevTools 显示注入样式 body 的 font-size:75%，根字号为 15px。原 YouTube 内嵌面板内容已查看，但没有取得其 iframe 内计算样式，不能混称同一次测量。

0.2.1 显式固定正文为实测的 11.25px，避免宿主注入规则造成独立验证页与扩展显示不同。模块标题仍 16px，主题及知识条目标题仍 15px。关于进一步改为主题 13px 或正文 13px，尚待用户选择；本次不据此擅自调整。主题行采用时间左、标题右的两列网格，顶部对齐，间距 8px，时间标签内边距缩小；长标题仅在右列换行，不截断。

知识清单两项标签加粗，各自下方列项目。划词笔记保留标题、段落、列表和加粗，沿用选区来源正文字号及产品行距；不克隆任意网页样式。摘录可直接编辑，有加粗、列表、小标题、正文四个轻量工具；个人理解和疑问继续选填，四个底部操作保持同排。


## 0.2.2 用户确认的阅读排版

逐字稿中英文（包括单语模式）统一字号 12px、行高 20px；段落时间保持 12px。全片总结、全片切片判断、知识清单、金句、有效方法五个模块标题统一为 16px／600／26.72px，前加 4px × 18px 的主题色竖条。片段、知识点和有效方法小标题统一为 15px／600／22.5px。其余正文、标签和前置知识保持原样。版本统一 0.2.2；本次仅排版调整，不重新生成 AI 内容。


## 0.2.4 编辑区视觉与回答设定

沿用 0.2.3 的文字尺寸层级，增加下列视觉规则。

| 字段 | 字号 | 字重 | 行高 | 视觉规则 |
| --- | ---: | ---: | ---: | --- |
| 排版工具按钮 | 11.25px | 400 | 23px | 文字 --accent，浅底 --tint，边框 --line |
| 摘录标题右侧“已手动编辑” | 11.25px | 400 | 18.79px | 主题色文字及浅色底，内边距 2px 6px |
| 理解／疑问字段标签 | 13px | 600 | 20.72px | 前置现有 18px 灯泡／问号图标，间距 5px |
| （选填） | 12px | 400 | 20.04px | 括号区分，与标签同排 |
| AI 回答章节标题 | 13px | 600 | 20.72px | 前置 9px 主题色菱形，间距 6px |
| 回答要求输入框 | 13px | 400 | 20.72px | 最小高度 120px |

手动编辑状态只在摘录标题行右侧显示一次，不再重复放在摘录下方。AI 折叠标题文字使用主题色。0.2.5 起仅编辑表单的 AI 折叠区使用 3px 主题色左线、8px 圆角、8px 10px 内边距；笔记展示卡片内去掉这层边框、圆角和内边距，避免与外层卡片竞争。两处仍保留上下 12px 间距。识别摘录内容、视频依据、通俗解释、准确概念、补充解释、具体例子、适用边界、直接回答等常见章节，兼容 Markdown、加粗和编号写法；图标不进入回答原文或导出。已有摘录富文本不套用 AI 章节识别。

AI提问窗口增加折叠的“回答设定”：修改立即用于本次问题；主动点击保存为默认后写入本机；恢复内置要求先只影响本次，保存后才替换默认。默认组织为结合摘录直接回答，再按需给通俗解释、准确概念、具体例子、适用边界，不强求无关栏目，不伪称看过视频画面。最多 2000 字符，默认读取完成后才可发送；读取／保存失败有提示，不覆盖已有回答。

### 历史记录页面设计样式（0.3.0）

沿用 `-apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', sans-serif` 系统字体栈。下表为 CSS 像素；浏览器缩放或截图缩放会影响肉眼看到的大小，不改变这些参数。

| 字段 | 字号 | 字重 | 行高 |
| --- | --- | --- | --- |
| 左栏“历史记录”标题 | 16px | 600 | 26.72px |
| 搜索输入 | 13px | 400 | 20.72px |
| 视频列表标题（最多两行） | 13px | 600 | 20.72px |
| 内容状态、笔记数量、最近学习日期 | 11.25px | 400 | 18.79px |
| 笔记命中摘要 | 12px | 400 | 20.72px |
| 右侧视频标题 | 15px | 600 | 22.5px |
| 逐字稿／视频脉络／笔记页签 | 13px | 默认 400，选中 600 | 20.72px |
| 阅读正文、脉络标题、笔记与编辑表单 | 复用本文 2.2 已确认参数 | 同左 | 同左 |

- 布局：左栏 280px；右侧文本最大 880px，居中；左右独立滚动，右侧头部与页签固定。常规两侧内边距至少 24px，窄屏 16px。
- 窗口不超过 760px 时，列表采用可收起浮层，选中视频后自动收起；宽屏也可手动收起。
- 浅色列表 `#f6f7fb`，阅读区 `--surface`，选中背景 `--tint`、左竖线 `--accent`；搜索高亮浅色 `#f6e6ad`，深色 `#5b502f`。其余沿用现有主题变量。
- 来源时间仍可点击；历史逐字稿的普通正文点击仅用于阅读，时间按钮才打开视频。
- 样式实现：`extension/src/history/history.css`；阅读组件复用 `extension/src/ui/`。真实 Chrome 已核对布局；隔离页面计算样式核对了左栏 280px、标题、搜索框与页签参数。
