# YouTube 学习笔记插件测试反馈记录

> 当前状态：模板保留，末尾追加独立技术预检记录；尚未执行产品测试或人工验收。
> 用途：按 [测试方案](./test-plan.md) 逐轮记录实际结果、修复与回归。复制下方模板新增轮次，保留原始记录，不覆盖历史。
> 填写规则：未执行保持“未执行”；缺少条件填“阻塞”；未知原因填“待定位”，推测标“假设”。所有示例栏均为空白，不代表已经发生。

## 一、交付目标与范围记录

| 项目 | 待填写 |
| --- | --- |
| 项目负责人 | |
| 目标 GitHub 账户或组织 | |
| 目标仓库名称及 URL | |
| 仓库可见性及确认依据 | |
| 目标分支与 remote | |
| 仓库确认日期与证据 | |
| 首版支持页面范围 | |
| 明确不支持的页面或功能及依据 | |
| 待确认的产品规则 | |
| 真实服务测试所需条件（不填密钥） | |

## 二、测试轮次模板

### 轮次信息

| 项目 | 待填写 |
| --- | --- |
| 轮次编号 | |
| 开始／结束日期 | |
| 执行人 | |
| 本轮目的及影响范围 | |
| 对应计划／设计／技术方案版本 | |
| Git commit／未提交变更说明 | |
| 扩展版本、构建标识及产物校验值 | |
| 操作系统／Chrome 版本 | |
| 窗口尺寸／缩放／主题 | |
| 逐字稿语言／快捷键绑定 | |
| 本机组件版本、签名与安装登记状态 | |
| Chrome 重启／组件升级后的系统授权提示次数与场景 | |
| 钥匙串锁定、拒绝授权及恢复结果 | |
| 在途请求中断、批次恢复与计费状态核对 | |
| 外部服务／模型标识（不填凭据） | |
| 服务连接代号／配置版本／翻译与分析模型路由 | |
| 凭据来源及系统存储是否可用（不填密钥或令牌） | |
| 真实接口或替身／缓存状态 | |
| 网络及账号可访问性（不填敏感信息） | |
| 使用的样本编号及有效性 | |
| 本轮命令或人工操作入口 | |

### 用例结果

状态填写：未执行／通过／失败／阻塞／不适用。各项必须关联可核对的实际结果，不填写笼统的“全部正常”。

| 用例编号 | 优先级 | 状态 | 实际结果 | 证据位置 | 问题编号 | 阻塞或不适用的依据 |
| --- | --- | --- | --- | --- | --- | --- |
| | | | | | | |

### 性能与外部服务观察

| 场景／样本 | 数据量及视频长度 | 缓存／真实服务条件 | 实测耗时或其他指标 | 对照拟定目标 | 结论与证据 |
| --- | --- | --- | --- | --- | --- |
| | | | | | |

真实额度消耗、失败码、服务限制及当前可确认结论：


### 本轮结论

| 项目 | 待填写 |
| --- | --- |
| 计划执行用例数 | |
| 通过／失败／阻塞／未执行／不适用数量 | |
| 未覆盖或不能下结论的内容 | |
| 本轮是否满足阶段出口及依据 | |
| 需要补充的测试 | |

## 三、问题与修复模板

每个真实问题复制一份。没有发现问题时不生成虚构缺陷。

| 项目 | 待填写 |
| --- | --- |
| 问题编号与标题 | |
| 状态（待定位／修复中／待回归／已回归／重新打开） | |
| 首次发现日期与轮次 | |
| 优先级及影响 | |
| 关联用例 | |
| 发生环境、视频/样本、版本 | |
| 前置条件 | |
| 稳定复现步骤 | |
| 期望行为 | |
| 实际行为 | |
| 截图／日志／录屏／复现产物路径 | |
| 影响到的数据与范围 | |
| 已确认根因及证据 | |
| 待验证假设 | |
| 修复方案 | |
| 实际修改文件及行号 | |
| 修复构建／commit（尚未提交则记录构建） | |
| 可能受影响的相邻功能 | |
| 原失败用例回归结果与证据 | |
| 扩展回归用例与结果 | |
| 仍存在的限制或后续工作 | |
| 问题关闭依据、日期及执行人 | |

## 四、人工验收记录模板

> 与自动化测试分开。未取得用户明确反馈前，不得填“通过”；没有反馈原文或实际证据时，不得代用户签收。

| 项目 | 待填写 |
| --- | --- |
| 人工验收状态（待进行／进行中／未通过／通过／待重新确认） | |
| 测试人 | |
| 日期 | |
| 已测试构建标识及校验值 | |
| 对应源码 commit 或未提交状态说明 | |
| 本机组件版本 | |
| 视频样本及实际使用环境 | |
| 已执行 HUMAN 用例编号 | |
| 未执行项目与原因 | |
| 用户反馈原文 | |
| 反馈所在会话／文件／截图证据 | |
| 发现问题及关联编号 | |
| 是否明确认可该构建、认可范围 | |
| 人工验收后是否发生变更 | |
| 变更后的回归及重新确认记录 | |

## 五、剩余问题与下一步

| 项目编号 | 类型（缺陷／待确认／依赖／后续版本） | 内容与影响 | 负责人 | 下一步动作 | 完成依据或所需条件 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| | | | | | | |

## 六、Git 提交与 GitHub 推送记录

> 仅在全部适用测试通过、真实集成完成且用户明确人工验收通过后填写实际执行结果。门槛未满足时不提交、不推送。本模板不代表已获仓库可见性或目标地址确认。

| 检查项 | 状态／证据（待填写） |
| --- | --- |
| 所有适用测试已完成并通过 | |
| 不适用用例的范围依据完整 | |
| 真实字幕、转写、翻译和 AI 集成证据完整 | |
| 所有缺陷实际修复并回归 | |
| 用户明确通过的构建及反馈证据 | |
| 提交源码与已签收构建的一致性 | |
| 目标仓库、可见性、remote、分支已确认 | |
| `.gitignore` 敏感文件覆盖检查 | |
| `git diff --cached` 范围与秘密自查结果（不记录秘密值） | |
| 最终提交前是否存在未测试改动 | |
| 实际 commit、分支、提交时间 | |
| 首次 commit 与人工签收构建关联 | |
| 实际推送时间和结果 | |
| 远端 commit 核实结果 | |
| GitHub 仓库／分支链接 | |
| 提交或推送失败及后续处理 | |

## 七、最终交付结论

实际完成范围：


已知限制与未完成内容：


人工验收结论及证据：


提交、推送状态及证据：


下一步工作：


## 本地翻译验证记录（待实际执行）

| 项目 | 实际记录 |
| --- | --- |
| Chrome 版本、扩展执行文档与 API 支持 | |
| 英中语言对、首次激活及模型下载 | |
| 翻译质量、长视频等待与缓存复用 | |
| 本地失败时云端请求次数 | |
| 主动 LLM 范围、候选应用与修订保护 | |

## 2026-09-11：实施前技术预检

本轮只验证开发环境及独立夹具，不对产品用例宣告通过。

| 检查 | 实际结果 | 限制 |
| --- | --- | --- |
| 开发工具 | Node.js 26.3.1、pnpm 11.19.0、Swift 6.3.3，arm64 | 当前已安装版本，不等于已确定产品支持矩阵；未安装依赖 |
| Chrome 安装版本 | 152.0.7977.84（读取应用信息） | 未进行目标扩展内翻译 |
| 钥匙串只读接口 | 随机不存在条目返回 -25300，符合预期；不读取秘密、不写入凭据 | 不等于写入、重启读取或 Native Messaging 通过 |
| Swift 编译 | 修正弃用参数后，warnings-as-errors 编译通过并复测查询通过 | 仅编译只读探针 |
| 翻译夹具静态检查 | probe.js、background.js 的 node --check 通过；Manifest 文件引用检查通过 | 静态检查不是浏览器运行结果 |
| Ego Lite 预检 | about:blank：secureContext=false、Translator=undefined | 非安全空白页且非 Chrome，不能据此判定目标 API 不支持；任务空间已关闭 |
| Chrome 扩展来源验证 | 待加载 scripts/probes/translator 的验证扩展 | 未加载、未下载模型、未调用翻译，不标为支持或失败 |

### 已处理问题

- 问题：Swift 编译报告 kSecUseAuthenticationUIFail 已弃用。
- 依据：编译器建议改用 LAContext.interactionNotAllowed。
- 修复：使用 LocalAuthentication 的交互禁止上下文传入查询，避免系统密码提示。
- 回归：以警告视为错误重新编译，运行返回预期的不存在条目状态。

### 下一步

- 在实际 Chrome 加载本地验证扩展，点击样本翻译并记录语言对、首次下载、输出、耗时及失败状态。
- 若独立扩展文档可用，再验证用户激活、面板关闭和任务恢复；不能把页面成功外推为后台成功。
- 使用明确隔离的测试凭据验证钥匙串写入、读取、授权和组件生命周期；本轮没有收集真实 API key。
- 完成技术选择后建立锁定版本的 pnpm 工程，继续产品实现；不得因本轮预检通过而提交或推送。

## 2026-09-11：Chrome 本地翻译质量验证（进行中）

- 已按用户确认加载本项目的独立验证扩展，ID 为 cpjemlokndjkmjlimjdgdgdfekcaojpj；没有修改现有其他扩展。
- 实际 Chrome 扩展文档返回 secureContext=true、translatorAvailable=true。
- 用户点击路径下，en→zh 的 availability 返回 downloadable；仅此状态不证明发生了完整模型下载或下载体积。
- 使用 8 组人工编写的公开性质样本，不上传个人笔记；未调用 client=gtx 或云端 LLM。
- 目录选择自动化中 Return 未能确认路径；改用 KP_Enter 后成功。这是 UI 操作问题，不是翻译错误。
- 后续以页面实际结果追加记录；API 存在不等于模型已可用，更不等于翻译质量通过。

### 本轮实际完成结果

- 实际 Chrome 扩展页面已完成 8 组本地翻译，首轮模型准备进度到 1；第二轮语言对 available。
- 改为每条独立 DOM 显示，修复单个大 JSON 节点在 UI 读取中截断的问题；第二轮完整原文、译文及核对记录见 [本地翻译评估](local-translation-evaluation.md)。
- T03、T08 存在明显口语／习语意义错误；T05 编程术语错译；T01 用词不符合笔记语境。没有把接口成功记为质量通过。
- 当前决策：本地引擎未确定为默认，未调用或接入 client=gtx；等待用户指令。不是代码错误导致的译文问题，不以界面补丁声称修复模型质量。
- 用户人工质量验收：未进行。完整产品用例和性能验收：未进行。未提交推送。

## 2026-09-11：client=gtx 对照试验

- 用户明确授权使用原 8 组样本对照；试验细节见 [gtx 对照记录](gtx-comparison.md)。
- 初次直连 fetch failed／连接超时；按系统现有代理重试后 T01–T08 全部 HTTP 429，没有译文。
- 状态：连接尝试已执行；质量比较阻塞，不记为质量失败或通过。限制的具体根因待定位。
- 修复验证脚本：限流/拒绝访问后停止批次，记录 Retry-After；静态语法检查通过，线上没有再次调用来验证停止规则。
- 原始响应摘要保存在 artifacts/probes/gtx-comparison.json（本地产物，不提交）。没有修改本地原始评估结果。
- 未切换产品默认引擎、未提交或推送。下次只在限制解除后进行有意义的重试。

### gtx 结果解释修正

用户明确反馈其测试成功。原报告不能以本机脚本的 HTTP 429 推断通道不可用；仅确认本次请求失败，响应来源和环境差异待定位。原始失败数据保留。质量比较、全文速度比较均未完成，本地短句热态耗时不能当成整片速度。详见 gtx-comparison.md 的修正说明。

## 2026-09-11：首轮产品实现与代码检查

### 本轮范围

根据用户最新要求，停止独立翻译通道比较，建立并实现实际扩展。免费方案保留 Chrome Translator API 和 client=gtx 两种，首轮代码采用前者；LLM 为主动选择入口。没有继续请求 gtx，也没有重复查询空钥匙串。

### 检查结果

| 项目 | 实际结果 | 边界 |
| --- | --- | --- |
| TypeScript 工作区检查 | 通过 | extension、service、shared |
| ESLint | 通过，最后一轮无错误或警告 | 类型规范、未处理 Promise、React Hooks |
| Prettier | 通过 | 产品源码与构建脚本 |
| 业务单元测试 | 3 个测试文件，31 项通过 | 分段、定位、导出、地址限制、存储冲突、视频隔离、额度估算 |
| 产品构建 | 通过 | Chrome 扩展文件、Node 服务包、Swift 钥匙串可执行文件 |
| AGENTS.md 行数 | 148 行 | 未超过 250 行 |
| 基础秘密模式扫描 | 未发现匹配 | 仅覆盖常见 key 前缀和私钥标识，不视为完备安全审计 |
| Chrome 实际加载与交互 | 未执行 | 工具要求在加载本地扩展时确认，已发出确认，等待用户回复 |
| 真实 LLM／Supadata | 未执行 | 尚未在本项目设置页配置服务 |
| 系统钥匙串真实保存与重启复用 | 未执行 | 没有写入真实密钥；构建成功不代表该流程通过 |
| 人工验收 | 未进行 | 尚不可签收 |
| Git 提交／GitHub 推送 | 未执行 | 等最终构建测试和人工验收通过 |

最后完整运行的命令为 `pnpm check`，退出码 0。初次存储测试因 fake-indexeddb 依赖尚未安装失败；完成安装后重新运行通过，没有删除失败用例。

### 发现的问题与处理

| 问题 | 原因 | 本轮处理 | 验证状态 |
| --- | --- | --- | --- |
| lint 无法启动 | 当前 typescript-eslint 不支持 TypeScript 7.0 | 锁定兼容的 TypeScript 6.0.3 | lint 和 typecheck 通过 |
| 依赖下载中断 | Registry 请求超时；不据此推断包不可用 | 使用本机已有代理完成安装，不绕过包检查 | 安装完成，保留单一锁文件 |
| React 开发模式可能遗漏首次记录加载 | Effect 重新建立时留下旧视频标识 | 订阅初始化重置标识，异步回填检查存活及视频 | 类型与构建通过；浏览器回归待做 |
| 笔记写入与译文写入可能互相覆盖 | 多个操作基于旧整条记录写回 | 串行提交、笔记保存前读取当前记录、事务修订校验 | 存储冲突与视频隔离测试通过；多窗口 UI 待做 |
| 面板关闭消息来源检查不够精确 | 仅校验扩展来源字符串 | 增加 iframe window 来源校验 | 静态检查通过；实际 iframe 待做 |
| 服务地址切换可能误用旧密钥 | 配置与凭据更新不是同一事务 | 地址变化要求新 key；新 key 使用新钥匙串账户标识，设置文件加锁 | 代码检查通过；真实钥匙串待做 |
| 无字幕重复提交风险 | 多窗口可能确认同一请求 | 生成任务保留状态、确认绑定视频／时长／设置修订、排他提交锁；未知结果不自动重发 | 估算规则测试通过；生成服务集成待做 |

上表区分实际执行过的测试与仅完成代码审查的修复，不把待回归项目填写为已验证。

### 后续工作与签收空白

实施差距、代码入口和下一步以 [implementation.md](implementation.md) 为准。优先在加载后的本项目扩展中验证真实 YouTube 流程，再继续补齐自动加载翻译、任务恢复、长文分析和完整源差异审核。

- 实际 Chrome 扩展 ID：待填写。
- 验证视频与场景：待填写。
- 用户配置服务后的连通结果：待填写，不记录密钥。
- 新发现问题、复现步骤、修复文件与回归结果：待填写。
- 用户人工验收意见：待填写。
- 下一步剩余事项：待实测更新。
- 验收构建标识、Git commit、GitHub 目标与推送结果：待全部验收通过后填写。

## 2026-09-11：授权加载后的真实 Chrome 联调

- 用户明确允许加载后，已加载本仓库 extension/dist；扩展名称为“ YouTube 学习笔记”（界面名称无首尾空格），版本 0.1.0，ID 为 fjhphfklnngjhmhmogmembgeogabjfhk。
- 已执行 pnpm native:install，并在本项目独立设置页看到“本机组件已连接。密钥保存在 macOS 钥匙串。”这只证明本机通信成功，没有配置或读写真实 API key，不能视为钥匙串保存与复用通过。
- 验证视频：https://www.youtube.com/watch?v=UsB70Tf5zcE 。本项目的视频下方入口、面板、三种语言选项与视频标题实际出现。
- 点击“获取已有字幕”后出现“请在设置页选择对应服务”。已有字幕请求未取得可用内容，回退服务尚未配置；不能据此认定视频没有字幕。
- 在播放器页面只查看字幕 URL 的参数名称，观察到实际资源请求包含初始字幕地址缺少的 pot 等运行时参数。未输出、存储实际签名或凭据。缺少运行时参数是本次失败的待回归解释，尚不能视为已确认全部根因。
- 已新增 content/caption-tracks.ts，优先选择当前视频、当前语言、可信站点的运行时字幕请求地址，排除其他视频、翻译轨道及其他站点；未找到时保留原地址。临时签名仅在内存中使用。
- 同时收紧扩展页面来源校验，支持设置页 URL 的查询参数和锚点，拒绝相似路径及其他扩展来源。
- 最新 pnpm check 退出码 0：类型、lint、格式、构建通过；4 个测试文件、34 项单元测试通过。
- 浏览器回归尚未完成：Chrome 界面控制后续返回空控件树，截图不可用；界面恢复尝试未成功。最新构建尚未确认重新加载，字幕修复的真实效果、翻译、播放跳转、笔记及导出均不记为通过。
- 下一步：恢复 Chrome 界面控制，重新加载同一扩展并刷新测试视频，优先回归已有字幕获取，然后继续真实使用流程。无需再次请求同一扩展的加载许可。
- 用户人工验收：未进行。Git 提交与 GitHub 推送：未执行。

## 2026-09-11：LLM 设置可用性修复

- 真实页面确认：DeepSeek API 地址为官方根地址，模型字段为空，测试按钮禁用；顶部显示外部服务连接失败或超时。未读取或记录 API key。
- 转圈原因：共享样式为所有 disabled 按钮设置 cursor: wait，模型未填也会显示等待指针。改为不可点击指针，并在卡片内说明禁用原因。
- 新增连接删除入口；删除先作用于表单，保存后生效，同步清理该连接的模型分工。保留钥匙串条目与笔记，避免隐式删除凭据。
- 新增 DeepSeek 名称识别与一键推荐配置。未配置的默认地址可自动填入；已配置或自定义地址不会因重命名被覆盖。一键更换已有地址仍要求重新输入密钥。
- 模型列表改为显式下拉框，保留手工输入；每张卡片分别显示加载、空列表、成功和失败。模型测试最多等待 35 秒，不自动重复请求，超时提示服务端可能仍在处理。
- 修复 HTTPS 请求中 DNS lookup 回调未兼容 options.all 的问题。Node 自动地址族选择需要地址数组，原实现返回字符串；补充单地址及数组回调回归。是否是用户这次网络失败的唯一根因，仍待新版实际请求确认。
- DeepSeek 官方请求明确关闭深度思考；建议模型 deepseek-flash 来自本日读取的官方接入和价格页，仍允许改选。未以价格推断译文质量。
- 已通过旧设置页补全用户 DeepSeek 模型并点击保存，页面明确显示设置已保存、密钥已写入系统钥匙串，DeepSeek 与 Supadata 均显示已配置。未修改密钥内容。随后更新本机组件并刷新设置页；刷新后界面控件暂不可读取，实际模型列表及模型响应尚未验证成功。
- 最终 pnpm check 通过：5 个测试文件、40 项测试通过，类型、lint、格式和构建通过。源码已拆出 ProfileCard、配置操作与等待时限模块。
- 待回归：新版页面删除与保存、DeepSeek 获取模型列表、模型测试成功及错误展示、刷新后钥匙串复用。未提交或推送。

### 同轮后续实测结果

- 界面恢复后发现原后台仍为旧构建，带 #translation 的设置入口被旧来源校验拒绝。重新打开不带锚点的设置入口后，新页面正常读取配置。本轮未确认重新加载整个扩展后台，部署时仍需重新加载扩展，源码已包含锚点校验修复。
- 新版设置页显示删除按钮和 DeepSeek 推荐配置；刷新后保留已配置状态。
- 点击 DeepSeek 获取模型列表：页面先显示正在获取，随后明确显示已获取 2 个模型，并出现可用模型下拉框。真实请求成功，复用了钥匙串密钥，无需再次输入。
- 点击测试此模型：按钮显示测试中，请稍候；随后卡片和顶部显示模型已正常响应，按钮恢复可用。真实模型 deepseek-flash 响应成功，不代表视频翻译质量已验收。
- 已通过新版删除入口依次移除用户误加的两条“我的 LLM”空白连接，保留 deepseek 与 Supadata，点击保存后页面确认成功。
- 本轮实际服务成功结果取代前文的暂未验证状态；完整扩展后台重新加载与人工验收仍待完成。未提交或推送。

## 2026-09-11：设置页清空、服务商和费用设计调整

- 范围：只调整和验证设置，未继续视频流程验收；没有提交或推送。
- 新增 ClearableInput、providers、SubtitleCosts、设置页独立样式；服务商切换更新配置并使旧模型列表、旧临时密钥失效。
- 调整生成准备逻辑：未自定义费率时采用共享官方参考值 2 credits/分钟。没有提交实际生成任务，没有产生字幕生成消费；费用规则来自官方文档。
- Chrome 实际验证：新建本项目设置标签页，已有 DeepSeek 与 Supadata 配置正常读取；新增未保存测试连接，用小叉清空名称成功；填写智谱并离开输入框后出现官方地址和 glm-4.7-flash；再改 ChatGPT 后切到 OpenAI 地址，旧模型清空，说明不再显示 DeepSeek 或智谱。
- 已删除本轮未保存测试连接，未改写用户原有服务或密钥；没有执行新的 LLM 请求。费用导航按钮可定位费用卡，截图确认灰绿导航、三项费用和高级折叠区域显示正常。
- 已更新本机服务组件，未读取或写入密钥；设置构建位于 extension/dist。
- 本机 Codex 可行性：已检查版本及登录状态，未读取 auth.json，未调用模型。当前记录方案，不声称插件已接入。
- 深色主题、窄屏、所有字段键盘交互与更多服务商真实连通仍未完整验收；单一窗口截图不能替代这些场景。
- 最终 pnpm check 退出码 0：5 个测试文件、42 项测试通过；类型、lint、格式与构建通过。测试新增跨服务商切换、未知名称保留、自选模型保留及 Codex 不误识别为 OpenAI API 的回归。

## 2026-09-11：保存状态与本机 Codex 接入

- 保存状态改为比较当前配置与最近一次成功加载／保存的快照，临时 Key 非空也视为修改；测试本身不改变保存状态。测试反馈不再附带固定“未保存”文案，保存全部后更新快照，所有卡片同步显示。
- 单元回归覆盖无修改测试、参数修改、密钥修改、恢复原值、保存后更新快照与旧配置兼容。
- 已新增独立“连接本机 Codex”入口；仍使用 llm 分工，connection=codex 表示本机接法，无需地址或 Key。检测按钮通过 codex login status 检查 ChatGPT 登录，测试及翻译／摘要通过同一本机适配器调用。
- Codex 以绝对安装路径启动，shell=false，提示从 stdin 传入；只读沙箱、禁止审批、不加载用户配置、关闭 shell／应用／插件／浏览器等功能，使用独立工作目录；输出只在内存解析，任务最长两分钟，不读取或复制 auth.json。
- 初次本机协议测试遗漏请求 id，被协议校验拒绝，没有调用模型；补齐 id 后进行实际调用。该测试直接通过已注册的本机组件，不冒充 Chrome 按钮回归。
- Chrome 已加载新版设置，原有 DeepSeek 显示已保存；进一步点击时用户切换页面导致工具拒绝，未将该次按钮测试记为完成。

### 本轮最终结果

- Codex 首轮实际调用未完成；加入可识别的脱敏错误分类后，明确为服务连接中断。随后检查仅确认系统代理启用状态，没有记录代理地址或凭据。
- 在 Codex 子进程中启用当前 CLI 支持的 respect_system_proxy 后，实际已注册本机组件的 probe 请求成功返回 {data:{ok:true}}，模型为本机配置中使用的 gpt-6-astra。复用 ChatGPT 登录，没有 API Key。
- 通过本机组件的 settings / saveSettings 协议保存“本机 Codex”试用连接，保留原有 DeepSeek、Supadata 和模型分工，没有自动改默认翻译或分析服务。
- 子进程独立进程组，超时终止该调用的进程组，避免 Node 启动器退出后留下 Codex 子进程；原始诊断只在内存用于固定错误分类，不输出到界面或日志。
- 最终 pnpm check 全通过：6 个测试文件、48 项测试通过；类型、lint、格式、构建通过。本机服务组件已更新。
- 浏览器只确认初始保存状态与入口已显示；后续浏览器持续切换到其他页面，工具拒绝继续操作。未声称“修改→测试→保存”全部 UI 点击回归已完成。本机 Codex 成功为真实本机协议集成结果，不冒充 Chrome 按钮实测。
- 使用时重新打开设置页可读取新构建和新增连接；本轮没有继续视频质量验收，没有提交或推送。

## 2026-09-11：Codex 重复项与模型名称显示

- 读取非敏感配置字段确认：存在两条本机 Codex，一条 model=gpt-5.5，另一条 model 为空；模型分工只显示 name，所以同名连接无法区分。没有读取密钥。
- 通过已注册组件的 settings / saveSettings 协议移除 ID 为 4d5a1c65-3dd2-44ab-9659-ca0cd7394173 的空白条目，操作前核对该条目未被翻译、分析或字幕分工引用。保留用户 gpt-5.5 连接及原任务分工。
- 本机连接已有时入口改为查看并定位，不再新建；函数式状态更新也检查已有连接，防止快速重复点击。后台保存同时拒绝重复本机连接。
- 模型分工选项显示连接名称与完整模型名，卡片另显示当前模型；为空明确显示未选择模型。模型输入使用普通文本，只有 API Key 使用掩码。
- 新建本机连接以用户本轮偏好的 gpt-5.5 为初始值；已有模型不自动替换。后台保存及 Codex 启动都拒绝空模型，每次显式传递 --model，不再采用隐含默认。
- pnpm check 退出码 0：7 个测试文件、50 项测试通过，类型、lint、格式、构建通过。本机组件已更新；本轮不重复调用付费模型，不将文本展示检查当模型质量验收。

## 2026-09-11：扩展图标替换

- 原因：manifest 未声明图标，Chrome 使用默认字母图标。
- 修复：按用户参考制作渐变播放图标，加入应用及工具栏 icons 声明，两个页面增加 favicon；不改变权限与设置逻辑。
- 验证：pnpm build 成功；构建目录中全部 16、32、48、128px PNG 存在且宽高符合声明；两页 favicon 引用通过检查。已查看 128px 缩略图。
- 本轮为静态图标替换，未重复调用模型或执行业务测试。Chrome 重新加载后的实际显示及用户外观验收待完成；未提交或推送。

## 2026-09-11：扩展重载后的上下文失效

- 用户确认新图标重新加载后符合预期，图标外观人工验收通过。
- 实际 Chrome 的本项目错误页（fjhphfklnngjhmhmogmembgeogabjfhk）显示两条 Uncaught Error: Extension context invalidated.；展开第一条定位 YouTube watch 页及 content.js:55。第二条同名，未展开核对其堆栈。
- 根因：内容脚本轮询直接调用 runtime.sendMessage(...).catch(...)，调用本身同步抛错时不会进入 Promise 的 catch；已有页面中的旧脚本在扩展重载后仍可能执行。
- 修复：新增 runtime-session.ts，统一检查 runtime.id、捕获同步抛错及异步拒绝；失效只清理一次，停止轮询、撤销页面事件监听和旧 UI。面板地址在有效启动时取得，按钮及消息入口检查会话状态。
- 回归：新增 5 项用例；pnpm check 退出码 0，8 个文件、55 项测试通过，类型、lint、格式和构建均通过。
- Chrome 实测限制：读取错误后用户切换浏览器，工具报告用户改变应用并拒绝后续点击；再次读取时为其他视频窗口和菜单。未清除历史错误、未确认最新构建重载后的错误是否复发，不以单元测试冒充浏览器回归。
- 下一步：重新加载本扩展，然后刷新需要测试的 YouTube 页，使旧脚本退出并载入新版；清除已记录的历史错误后，验证播放、打开入口和再次重载是否产生新记录。再开展完整流程验收。未提交或推送。

## 2026-09-11：自动准备、译文应用、长视频和笔记同步

- 用户截图 1 为候选译文确认界面，并非接口报错。旧逻辑所有 LLM 译文都进入候选区；改为每批成功后自动应用未手改且版本一致的段落，冲突部分展示现有／新译文，用户明确确认后才替换译文，保存前再次校验版本。
- 字幕与本地翻译原先只有手动按钮。新增 usePreparation：视频首次加载自动抓取已有字幕；中文或双语模式自动翻译缺失且未手改的内容；原文模式不自动翻译，失败不无限重试、不自动使用付费 LLM。快捷窗口只补字幕，不重复启动本地翻译。
- 新增覆盖面板内容区的 LoadingView：阶段说明、渐变进度条、骨架占位；未知进度显示循环条，实际完成比例才显示百分比；适配深色与减少动画偏好。首次模型准备若被 Chrome 要求用户激活，明确提示点击重试，不能绕过浏览器限制。
- 长视频原先按整个 segments JSON 超过 180000 字符直接拒绝。现在只按原文、ID、时间计算请求长度，每批不超过 24000 字符，扩展逐批请求并显示进度；服务校验每批时间及引用。按时间合并主题与分批总结，去重金句／方法；不会截断整片内容。单个异常超长段落仍明确报错，不静默删字。摘要为分批总结顺序合并，尚未增加额外的一次全局润色请求。
- 快捷笔记原先只更新弹窗中的缓存，主面板没有保存通知。后台成功保存后发送视频 ID 与修订号；主面板按视频重新读取记录并忽略旧版本／其他视频的通知。普通保存先读取最新记录，避免翻译写入时覆盖另一窗口新笔记。
- 自动化：新增自动启动去重、原文模式、LLM 即时应用、手改保护、迟到结果、21 万字符原文全量分批、笔记通知刷新等回归。曾因测试根目录与 extension 的 React 模块解析不同导致两项测试未替换 Hook；修正测试模块路径后通过，未关闭测试。
- 浏览器验证：当前 Chrome 在用户操作时切换，工具拒绝新建检查标签，因此本轮未完成产品内点击回归、加载样式截图和真实服务长视频质量验收，不把模拟接口测试当作实测。
- 本轮审查 main.tsx 仍超过 300 行：保留现有编辑、导出和选择交互，避免同时改动已在验收中的操作；新增自动准备、进度视图、结果应用与分批规则已独立成模块。后续布局整理可单独拆分。
- 最终验证：pnpm check 退出码 0，12 个测试文件、68 项测试通过，类型、lint、格式及构建通过。新增本地授权提示时 lint 曾提示缺少原始 cause，补齐后全量复查通过。已执行 native:install 更新本机组件，未读取或写入 API Key；尚未提交或推送。

## 2026-09-11：0.1.1 版本与交互修复

- 用户截图为统一的引用／时间校验失败；截图无法区分具体哪一个字段失败。确认旧代码同时校验模型生成的毫秒时间和金句精确子串，这两类不一致均会拒绝整份结果。本轮没有声称已读取到模型原始错误响应。
- 新提示词让模型选择片段编号，服务端映射回原始毫秒范围和原文。无法定位的个别引用不输出并显示省略提示，主题全部无效仍拒绝保存，未关闭校验伪装通过。
- 面板由移除 iframe 改为隐藏并保留，收起显示名称条；重新点击入口始终展开。被 YouTube 替换的节点可重新挂载；BFCache 页面隐藏不直接永久停止脚本，重复注入清理旧轮询和消息监听。
- 工具栏点击检查当前 YouTube watch 页，接收端失效时注入最新脚本恢复。扩展安装／更新尝试恢复已有视频页；新增 scripting 权限，站点仍仅 YouTube。
- 删除按钮加入笔记列表和编辑窗口；用户逐条确认后持久化删除并广播刷新，无批量清库。独立阅读增加返回来源按钮；主面板去掉标题与常驻翻译按钮。
- 新增引用编号、精确时间来源、无效引用、围栏 JSON、连续展开收起 10 次、DOM 脱离后恢复、重复注入去重、工具栏消息、BFCache 与笔记删除回归。
- Chrome 尝试：新建检查标签成功，下一步地址栏操作时工具报告用户切换应用并拒绝继续。因此尚未完成实际重载后回归和真实模型结果验收，没有改动用户其他页面。
- 最终 pnpm check 退出码 0：14 个测试文件、76 项测试通过，类型、lint、格式、构建及版本一致性检查通过；构建 manifest 为 0.1.1。native:install 已更新本机组件，未读取或写入 API Key。AGENTS.md 为 153 行，低于 250 行。未提交或推送。

## 2026-09-11：0.1.2 展示、字幕缓存和短总览

- 原本已有 IndexedDB 视频缓存与读取优先逻辑，不据截图断言每次一定重新计费。确认的缺口是字幕结果依赖面板接收后才写入；新增后台缓存服务，在返回前持久化，合并同一视频请求。保存时读取最新记录，冲突最多重试保存两次，不重复调用字幕接口；面板收到迟到字幕时保留已有段落和译文。
- 新增缓存用例：同时请求只抓一次、缓存服务重建后仍命中、保存冲突不重复抓取。长视频用例增加最终简短总览断言。
- 加载容器原为多个可收缩 flex 子项，空间不足时会挤压图标、进度条；改为整组 grid 居中并禁止内部项目收缩。笔记分组按钮、统一正文字号并对齐图标。移除顶部版本文字；面板根据双栏位置向播放器顶部对齐，未改变 YouTube 其他节点样式。
- 分批分析后新增一次只输入分批摘要的精简请求，目标 2～3 句约 100 字；失败不丢主题，显示提示并折叠长总结。用户触发整理时才执行，打开缓存不自动重新整理。
- 原分段规则保持不变；已记录断句问题及待确认方案。真实 Chrome 位置与加载外观、关闭浏览器重启，以及真实模型精简质量尚未实测，不能写为人工验收通过。
- 最终 pnpm check 通过：15 个测试文件、78 项测试，类型、lint、格式和构建通过；版本 0.1.2，本机组件已更新。未提交或推送。

## 2026-09-11：0.1.3／0.1.4 标题和笔记视觉调整

- 0.1.3 将视频标题与设置／收起放到同一顶部行，移除原本单独操作行产生的空白；设置齿轮改为八向对称 SVG。
- 0.1.4 按用户新参考提取 NoteCard 组件，加入蓝色左边线、右上时间和浅灰原文引用块；理解在上、中文及疑问在下，保留统一正文字号和右对齐操作，包含前一构建的标题调整。
- 最终 pnpm check 退出码 0：15 个文件、78 项测试通过，类型、lint、格式和构建通过，构建版本 0.1.4。本轮为 UI 调整，未改业务数据结构或本机服务逻辑，未调用外部模型。浏览器实际外观尚待用户验收，未声称已截图验证。未提交或推送。

## 2026-09-11：0.1.5 操作与组合导出

- 跟随使用原回调，移动到底部右侧；语言下拉换为三按钮；去掉当前段翻译、简化整片按钮名称，保留独立阅读。
- 面板对齐由负 margin 改为 transform 并补偿后续间距，避免顶部外边距折叠；实际 YouTube 几何位置仍待截图验收，不能仅凭代码宣称对齐已目测通过。
- 导出在本地按选择组合内容，Markdown、TXT 和 Word 直接下载；Word 为无压缩 ZIP 封装的真实 OOXML，不伪装为 .docx 的 HTML。PDF 使用浏览器打印窗口保存，不声称已直接自动下载 PDF。
- 新增内容选择、语言过滤、TXT／Markdown、XML 转义及 DOCX 解压／CRC／关系文件回归。PDF 实际另存和 Microsoft Word 打开排版尚未执行，不以 ZIP 完整性替代应用验收。
- 全片总览与主题介绍分开，旧长总览显示重新整理提示而不混入全片简短总结。未重写缓存内容、未改变分段规则、未调用外部模型。


## 2026-09-11：0.1.6 笔记图标替换

- 按用户选定的 02 组方案，在共用 SVG 组件中绘制笑脸黄色灯泡、带腮红的粉色红问号气泡，覆盖卡片与编辑窗口。
- 本机 pnpm check 通过：类型、lint、格式、16 个测试文件共 81 项单元测试及扩展／本机组件构建通过。此次纯视觉改动未新增业务测试。
- 尚未在真实 Chrome 中加载新构建核对视觉，需重新加载扩展并刷新视频页后由用户验收；未提交、未推送。
- 最终 pnpm check 退出码 0：16 个测试文件、81 项测试通过，类型、lint、格式和构建通过，版本为 0.1.5。未完成真实 Chrome 视觉与文件应用验收；未提交或推送。

## 2026-09-12：0.1.7 纯本地规则分段

- 用户要求先完成统一重组、可靠断句、向后比较及长度软目标，按实际效果再讨论 SaT；本轮没有引入 SaT、音频转写或新依赖，没有调用真实供应商或读取密钥。
- 根因复核：YouTube 路径原本以 240 字符、20 秒、1400ms 间隔限制合并；供应商已有及生成字幕没有统一重组。先新增回归，旧实现的长句、时间空隙、条目内重新断句三项失败，再替换算法并统一新字幕入口。截图文字复现不等于读取到了截图视频的原始响应。
- 本地规则支持常见缩写、小数、网址、成对引语、可靠说话人边界，使用有界动态规划比较后续完整句。默认英文 220／中文 100 字符为软目标；缺标点时保留长段。当前实际字幕适配没有可靠说话人标签，不宣称已经识别访谈换人。
- 新增来源子串范围、算法版本和旧稿备份可选字段；旧记录无需清库。普通打开复用已有缓存，用户显式预览应用才改变旧段；手改段保持原样，未变边界复用译文，变动译文在旧稿备份中保留，新段通过“补齐本地翻译”翻译。笔记快照及个人输入不自动改写，源标识变化也提示差异。
- 保存回归覆盖视频切换、预览后翻译修订、并发写入拒绝、独立笔记更新保留、持久化重读与恢复、旧缓存不请求、来源范围契约和时间重叠定位。未改变生成计费确认流程。
- 按新需求更新旧测试：“每个句号必分”和“字幕长间隔必分”不再成立，改为完整短句可合并、时间间隔不能单独切开缺标点文本。新增引语测试初始将很短的下一句强制独立，和长度软目标不一致；调整为足够长的下一句，保留引语完整和闭引号后可分两个断言。
- 最终 `pnpm check` 退出码 0：类型、lint、格式、18 个测试文件共 101 项测试、扩展及本机组件构建全部通过；根及三个工作区 package.json、源码和构建 manifest 版本均为 0.1.7。无依赖升级，未提交、未推送。服务端行为未变，未重新登记本机组件。
- Ego Lite 本地浏览器夹具验证：截图文字两段预览为一段；取消后仍为两段；应用后为完整句且旧稿备份为两段；恢复带回旧原文和两条旧译文；模拟保存失败时仍为原两段、预览保留且显示错误。使用真实算法和弹窗组件、内存保存夹具，不代表真实 Chrome 扩展或供应商验收。
- 浏览器夹具初次启动因测试目录不能解析工作区 React 包失败，增加测试专用 Vite 配置定位已安装的依赖后通过。截图调用 `Page.captureScreenshot` 超时，未取得视觉截图，未声称视觉验收通过。任务浏览器空间及测试服务器已关闭。
- 同机 Ego Lite（浏览器报告 Chrome/150.0.0.0）开发模块测量：15000 条合成英文字幕重组为 3749 段耗时约 390ms，10000 次播放定位含首次索引约 1ms。此为单次合成样本测量，测量时尚未追加成对引语保护，数据无引语；不代表最终构建、真实视频、全部列表渲染或 300ms 交互目标达成。最终构建的 15000 条保真回归已通过。
- 待用户验收：在 Chrome 重新加载 0.1.7 并刷新 YouTube，用原有问题视频预览／应用分段，补齐本地翻译，检查读起来是否完整、播放跳转及跟随是否符合来源精度；关闭浏览器重启及真实双标签交互仍待实测。不要删除缓存来试新版，可从预览窗口恢复上次逐字稿。

## 2026-09-21：三组真实逐字稿分段问题复核（仅诊断）

- 用户提供 Rebecca、Greg Brockman、Jessica Wu 三组分段前后导出。当前算法复现出的英文分段逐段与用户结果一致，段数分别为 105→1、458→252、105→94；忽略空白后英文无增删。
- 确认两类根因：缺标点单元没有备用句子边界；无界引号保护错误地吞并远处正文。Rebecca 的两个点号均为小数点，整片没有句末候选；Greg 28:08–42:49 有 115 个句号、39 个问号，却被远距离引号误配屏蔽。
- 隔离副本仅取消引语内部断点屏蔽后，Greg 从 252 段变为 318 段，最长段由 17045 字符降至 1264；另两组不变，验证缺标点问题仍需独立处理。此实验未修改生产代码。
- 另外发现 Greg 的 `>>` 轮次标记未利用；旧时间区间较粗，后续修复须同时保留来源范围、递增分段算法版本，不能只改常量或在粗时间大段上重新分割。
- 本地诊断用例 1 项通过；未执行生产构建或实际浏览器验收，未引入 SaT，未提交或推送。详见 [三组样本复核](segmentation-review-2026-09-21.md)。用户文件只读；完整样本和诊断脚本留在忽略目录 artifacts/segmentation-audit/。


## 2026-09-21：0.1.8 巨段修复与本地回放

用户授权按三组逐字稿诊断补齐规则。根因是无标点单元没有备用断点，以及无界引语保护屏蔽了远处句末。已限制短引用范围、补充分句与会话轮次候选，并从旧 sourceSpans 恢复细粒度来源；仍偏长及推断结果有提示。版本统一升级到 0.1.8，分段算法为 2，未引入 SaT。

实际环境：本机 Node.js 26.3.1、pnpm 11.19.0。修复前四类新回归均失败，修复后通过。首次完整检查因正则不必要转义的 lint 错误失败；修正后 `pnpm run check` 全部通过，19 个文件共 109 项单元测试及构建通过。额外真实三组回放 1 项通过，包含原文保真、来源映射、旧版升级、重复预览、备份恢复。

Rebecca 1→93 段，最长 23382→594 字符；Greg 252→397 段，最长 17045→563；Jessica 94→95 段，最长 697→319。仍有 4 段超过两倍软目标，已提示，不能以数量变化宣称句子完整性全部通过。来源粗时间可能重叠，本轮没有音频、Chrome 产品交互、翻译或全文语义验收。下一步由用户在新版实际插件复测，确认后再讨论是否需要 SaT。未提交或推送。

完整规则、逐文件用途、原始测量及复测步骤见 [修复记录](segmentation-repair-2026-09-21.md)。私人原稿和回放产物保持在 .gitignore 排除目录，未对外发送。


## 2026-09-21：用户确认分段效果与 0.1.9 快捷键

用户提供第二次修改后的三个本地导出，并明确 Rebecca、Greg、Jessica 三个视频分段均符合预期，未测试其他视频。只读核对时间段标题分别为 93、397、95，与此前回放一致。此处记录为这三个视频的阅读效果确认，不扩展为所有视频或音频时间精度验收。本轮保持分段代码不变，SaT 保留为后续备选。

新增两个独立快捷操作和设置入口。Chrome 官方 Commands 接口不提供更新绑定方法，真实绑定由用户在 Chrome 快捷键页修改并自动保存；插件设置展示实际值，不存伪配置。设置页可在本机组件离线时使用快捷键分类。

本机 Node.js 26.3.1／pnpm 11.19.0：`pnpm run check` 通过类型、lint、格式、20 个文件共 114 项单元测试及构建。新增 5 项验证：两个命令独立、事件标签优先、缺失标签回退与空窗口、站外及 Shorts 范围、保存失败向上传递且不重试。manifest 中两个建议按键不同，旧 capture-note 名称保留，权限不增加。

本轮未执行真实 Chrome 按键、修改绑定、焦点返回刷新或视觉浏览器验收，也未测试系统快捷键冲突。用户复测：重载扩展为 0.1.9、刷新 YouTube 页，在“设置 → 快捷键 → 修改快捷键”分别指定按键，返回核对实际值；关闭主面板分别按键打开面板／笔记窗，在其他网站确认无笔记副作用。现有草稿应继续保留。本轮未提交／推送，未重新登记本机组件。


## 2026-09-21：0.1.9 快捷键用户验收与首次 Git 交付

用户明确反馈快捷键功能测试可实现，并授权将当前版本提交到本地 Git，推送到 git@github.com:monical2026/YouTube---.git。结合上一轮三组视频分段的用户确认，交付版本保持 0.1.9，本轮无功能或构建变化。

推送前核实：本地 main 尚无提交；指定 GitHub 仓库存在、为空且可见性为 PUBLIC。沿用 main 首次提交，不创建新仓库、不改变可见性、不强推。忽略私人逐字稿及 artifacts、node_modules、dist、真实 .env 和私钥；待提交文件扫描未发现凭据匹配。提交和推送的实际哈希及结果以本次交付回复为准。

用户验收范围为本次分段与快捷键实际使用，不扩展为历史尚未执行的其他服务、系统或全量视频验收。
