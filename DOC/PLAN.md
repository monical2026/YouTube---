# YouTube 学习笔记插件实施计划

> 状态：已停止独立翻译对比预检，进入产品实现；首轮可构建代码已建立，实际浏览器联调及最终验收尚未完成。具体检查结果见 test-feedback.md，不代表产品已通过验收。
> 需求依据：当前会话中用户确认的要求。未确认的设计单独列出，不作为既定结论。

## 文档导航

本目录为项目文档目录 `DOC/`。项目介绍见 [README.md](../README.md)，项目级协作说明见 [AGENTS.md](../AGENTS.md)。同目录文档之间使用相对链接。

| 文档 | 用途 |
| --- | --- |
| [design.md](design.md) | 视觉风格、布局、字体尺寸、SVG 图标与交互状态 |
| [architecture.md](architecture.md) | 项目文件树、工作区、模块依赖与源码/数据存放边界 |
| [service-settings.md](service-settings.md) | 独立设置网页、LLM 与外部 API 字段、模型路由及安全保存 |
| [technical-design.md](technical-design.md) | 扩展架构、视频切换、数据与版本、服务接口及权限 |
| [test-plan.md](test-plan.md) | 开发后的测试步骤、验收标准和人工验收流程 |
| [test-feedback.md](test-feedback.md) | 待填写的测试结果、问题、修复、回归、下一步和人工签收记录 |

## 一、目标与版本范围

帮助用户在 Chrome 中看懂英文 YouTube 视频，通过逐字稿、中文 AI 内容梳理和个人笔记，减少暂停、重复观看及切换笔记工具的操作。

第一版仅供个人在当前电脑上使用，优先保证流畅性和笔记可靠保存。用户已确认第一版仅依据带时间的逐字稿进行翻译和 AI 梳理，不分析视频画面、PPT、图表或屏幕操作。教程仍可整理讲解文字，但不承诺补全只在画面中出现的操作步骤。画面理解留待后续评估，不作为第一版交付条件。未来公开产品时再支持多用户；跨设备同步、直接接入 Obsidian、对个人笔记进行后续 AI 整理放到后续版本。

## 二、已确认需求

### 1. 语言与显示

- 逐字稿提供“原文”“中文”“中英双语”三种模式，只展示当前所选模式对应的内容。
- 按所选模式处理需要的内容，原文模式不以完成中文翻译作为展示前提。
- AI 输出语言独立于逐字稿模式，默认固定为简体中文。
- 切换逐字稿语言不改变已有 AI 内容，不自动重新生成 AI 摘要。
- 笔记摘录保留中英文对照；个人理解与疑问按输入原样保存。
- 原文模式下尚无译文时，按实施默认先保存来源和输入，再补齐对应译文；未补齐前明确标记状态。

### 2. 字幕获取与加载

- 打开视频并启动插件后，先获取字幕，再按所选模式翻译和展示。
- 没有可用字幕时，每次先展示该视频的预计生成积分、估算依据和处理范围，用户点击确认后才向外部服务提交生成请求；取消后保留已有笔记。
- 抓取与处理期间展示加载界面、进度条及当前处理阶段。
- Supadata 作为优先验证的字幕服务候选，尚未完成接口实测或最终选型。
- 必须区分获取已有字幕、生成字幕、中文翻译和 AI 分析的能力与额度。

### 3. 逐字稿分段与播放联动

- 逐字稿由时间戳和文字小段组成，原文与译文保持对应关系。
- 使用本地程序整理阅读分段；2026-09-12 已确认统一重组、可靠断句、向后比较和长度软目标，具体阈值及实现效果待样本验证。
- 播放期间高亮当前段落；跟随状态下自动滚动到当前内容。
- 用户可以自由上下滚动，查看历史或后续内容。
- 手动滚动后停止自动拉回，但当前播放段落的高亮状态仍随视频更新。
- 底部提供“跟随”按钮；点击后定位当前播放段落并恢复跟随。
- 视频暂停时也能翻阅和点击“跟随”定位暂停位置。
- 直接点击逐字稿段落文字，视频跳到对应时间点并开始播放。
- 拖动划词与单击跳转必须区分，选中文字不得误触视频跳转。

### 4. 笔记摘录与输入

- 快捷键摘录当前播放片段，将双语内容及时间关联插入笔记。
- 主面板未打开时也能在当前 YouTube 视频页通过快捷键记录，使用独立轻量笔记窗；扩展本身仍需启用。
- 摘录后弹出窗口，允许补充自己的理解、想法或疑问。
- 阅读逐字稿时，鼠标选中文字后出现“做笔记”入口，点击即可摘录。
- 个人理解与疑问可以分别填写，也可以留空。
- 笔记保留视频来源及时间关联，便于后续回看和导出。
- 插件中的双语摘录直接上下显示，不显示“英文原文”“中文翻译”标签。
- 理解与疑问使用简洁 SVG 图标，不显示常驻的“我的理解”“我的疑问”标签。
- SVG、字体大小和布局采用 design.md 中的设计默认，实际观感仍需原型及人工验收。
- 保存后的笔记仍可编辑，包括摘录文本、理解和疑问；修改笔记摘录不反向修改逐字稿。

### 5. 编辑与摘录版本

- 逐字稿原文和中文译文均允许手动修正。
- 修改中文译文后直接保留修改结果，不触发重新翻译。
- 已保存的笔记摘录保留摘录时的原文和译文版本。
- 对应来源文字修改后，提示笔记来源已更新，不静默替换摘录。
- 用户可查看修改差异，自行选择更新引用或保留原样。
- 更新引用时，不自动修改用户自己的理解和疑问。
- 修改原文后的实施默认：保留已有译文，标记待核对，由用户手动触发重译，不静默覆盖人工修正。
- 若笔记摘录也已手动改过，更新来源时展示差异，由用户选择，不自动覆盖。

### 6. AI 内容梳理

- 提供整个视频的中文摘要及内容框架。
- 每个内容分段展示：内容标题、起止时间、内容介绍、解决的问题、应用场景。
- 提供全片与每段是否适合独立切片的粗判断及理由，详细切片范围规划后续再做。
- 提取视频中的金句及有效方法，并关联来源时间。
- 不使用“第一章、第二章、第三章”作为主要展示标题。
- 暂采用“视频脉络”作为设计默认栏目名，使用“时间范围＋具体内容标题”，可在视觉验收时调整。
- 用户已确认第一版只提供建议时间范围和理由，不剪辑、不生成或下载短视频文件。

### 7. 保存与导出

- 第一版在当前电脑保存笔记，刷新或重新打开视频后能够恢复。
- 支持导出完整逐字稿，提供原文、中文、双语选择。
- 支持导出个人笔记，保留双语摘录、理解、疑问、视频来源及时间信息。
- 导出默认采用 Markdown 笔记，以及 TXT/Markdown 逐字稿；属于可调整实施默认。
- 未来增加直接写入 Obsidian 知识库；当前不把文件导出等同于自动同步。

### 8. 页面入口与视频切换

- 插件仅用于 YouTube 视频；其他网页不显示插件页面界面。
- YouTube 视频下方的合适空余位置放置小入口按钮，点击打开主面板，避免遮挡播放器和已有操作。
- YouTube 内切换到不同视频时，逐字稿随当前视频更新，AI 内容和笔记也按视频隔离。
- 站内跳转、自动连播、播放列表切换、前进后退、直接刷新均须验证；同页切换不依赖整页刷新。
- 切换后旧视频的迟到请求不能覆盖当前视频；返回旧视频时恢复其自己的内容。
- 离开视频页时撤下入口、面板及笔记窗；保存中的草稿保持原视频归属，不转移到新视频。
- 多标签页各自跟随对应视频，快捷键只作用于当前活动的有效视频标签页。
- 用户已确认第一版支持普通 YouTube watch 视频页；Shorts、正在直播的视频及其他网站嵌入的 YouTube 播放器留到后续。普通视频的剧场／全屏表现仍需实测，不把它们等同于其他页面类型。

### 9. 开发、测试与提交顺序

- 先完成设计、技术方案、测试方案与空白反馈文档，再进入代码实现。
- 实现后按 test-plan.md 执行，在 test-feedback.md 记录结果、问题、修复方式、回归证据及下一步。
- 全部范围内测试通过且用户明确人工验收通过后，再 Git 提交并推送 GitHub。
- 测试未执行、受阻或未完成人工验收时，不记录为通过，也不提前提交推送。
- GitHub 目标仓库和可见性目前未确定，发布前再确认；当前仓库尚未配置远程地址。

### 10. 独立设置网页与模型连接

- 用户明确要求专门网页设置 LLM 及其他外部服务 API，在 Chrome 独立标签页打开，不依赖当前是否观看视频。
- 设置页可填写 LLM 连接名称、接口地址、API key、默认模型，提供模型列表或手动输入、测试连接与保存配置。
- 普通翻译优先验证 Chrome 本地 Translator API；LLM 翻译由按钮主动触发，可与 AI 梳理共用或分别选择模型；AI 输出仍默认中文。
- Supadata 作为独立字幕服务配置，凭据、字幕模式及测试与 LLM 分开。
- 页面填写的密钥经按需组件安全存入 macOS 系统钥匙串，环境变量仅保留开发/受管来源；不要求用户在源码中填 key。
- 第一版兼容协议、具体字段、错误与成本提示属于 service-settings.md 所列实施默认，不承诺尚未测试的任意供应商均兼容。
- 修改设置不自动重生成已有结果，记录配置版本；未配置 LLM 时仍可读已有原文和编辑本地笔记。

## 三、实施建议与质量约束

以下为用户授权完善计划后的实施默认与质量约束，不声称是用户逐条亲定。后续验证如需调整，记录原因。

- 分段优先使用句子边界、标点和停顿，合并过短碎片，限制过长段落；保留底层时间映射，避免编辑文字后跳转失效。
- 原文、译文、笔记引用使用稳定关联，记录来源版本和摘录快照；重新分段时也需明确旧笔记如何定位。
- 服务未提供真实百分比时使用不确定进度条和阶段提示；有可计算进度时再显示百分比，不虚构进度。
- 翻译与 AI 分析失败时支持清楚的错误提示及重试，避免重复请求导致重复计费。
- 保存可复用的处理结果，减少重复打开视频的等待和额度消耗。
- 金句须能回到原文核对；AI 推导的应用场景与讲者明确表达的内容应区分。
- 第一版切片建议仅依据逐字稿，明确判断依据，不宣称已检查画面效果。
- 图标支持悬停提示、键盘聚焦提示及辅助阅读名称，同时保持默认界面简洁。
- 外部服务凭据使用环境变量或安全凭据机制，不写入源码、导出文件或日志，不把共享服务密钥打包进扩展。
- 超额处理和自动充值策略须明确；不默认开启付费处理。
- 关闭面板只关闭显示，不关闭快捷键记录能力；不因隐藏面板自动启动字幕或整份 AI 分析。
- 快捷记录立即冻结视频与时间；字幕尚未取得时先保存想法和时间来源，随后补引用，不丢输入也不编造文字。
- 默认打开笔记窗不暂停视频，用户可手动暂停。页面切换时草稿自动保存并保留原视频关联。
- 编辑、划词与点击跳转严格区分，编辑笔记或逐字稿不得意外改变播放位置。
- 按视频、源文字版本及配置复用结果，修正人工文本时防止后台旧翻译覆盖。
- 清理缓存、卸载和跨设备备份的边界需要清楚说明；第一版不实现隐式清除个人笔记。
- 补充本地逐字稿搜索、AI 条目加入笔记和可查看的保存状态，均作为本轮完善的实施默认；搜索未生成的译文不得显示虚假的完整结果。
- 新摘录及编辑旧笔记先自动保存草稿，“完成”后形成正式笔记或新修订；关闭窗口保留草稿，“取消本次修改”恢复编辑前版本。
- 当前视频已有未完成笔记窗时，再按快捷键聚焦现有草稿，避免替换来源或丢输入；切换视频后可以建立新视频的草稿。

## 四、按影响程度排列的待确认事项

一次只向用户询问一个需其判断的问题；能够通过官方资料或测试查证的事项由开发过程解决。

| 优先级 | 事项 | 影响与当前状态 |
| --- | --- | --- |
| 1 | 无字幕比例和费用上限 | 已确认每周约 5 个视频，每个约 40 分钟、1 小时或更长；无字幕比例和预算仍未知 |
| 2 | 代表性访谈与教程样本 | 用于验证逐字稿质量和中文整理效果；不能假设用户仅看访谈 |
| 4 | 翻译与 AI 分析服务选择、具体供应商与模型 | Supadata 字幕能力不等于完整中文学习流程，需验证质量与成本；macOS 钥匙串及按需组件方向已确认 |
| 5 | 划词摘录的粒度与双语对应 | 精确选词和完整句段的双语对齐不同；设计先保留精确选区及其上下文，并将对应整段译文标为上下文，须用样本确认体验 |
| 6 | 普通视频的全屏体验及直播回放边界 | Shorts、正在直播和站外嵌入已确认后续再做；结束后的直播回放不等同于正在直播，需按点播能力实测判断 |
| 7 | 重装或卸载后的恢复与备份 | 当前电脑可用不代表已有备份策略；跨设备暂不做 |
| 8 | 代表性视频与人工验收 | 测试方案已写，但样本和实测证据尚未取得；尺寸和响应目标不等于已验证效果 |
| 9 | GitHub 目标与可见性 | 发布前需要具体目标，不能自行新建公开仓库 |

常规布局、字号、图标、弹窗、进度和数据隔离已在配套文档给出默认方案，不逐项打断用户确认。会改变使用成本、隐私范围或最终功能范围的取舍仍应一次问一个。

## 五、实施阶段

### 阶段 1：明确影响架构的边界并验证服务

- [x] 更新需求及编写 design.md、technical-design.md、test-plan.md、test-feedback.md（仅文档）。
- [x] 补充 architecture.md 和 service-settings.md，细化文件架构及独立服务设置网页（仅文档）。
- [x] 将项目文档统一整理到 DOC/，根目录建立 README.md 与 AGENTS.md；源码结构按已确认方案保持不变。
- [x] 包管理器统一为 pnpm，将项目规则文件改名为 AGENTS.md，并补充业务流程、高风险模块和修改边界，保持不超过 250 行。
- [ ] 先打通设置页、本机组件、凭据安全保存及指定模型连接测试，再接完整视频处理。
- [ ] 按待确认事项顺序逐项沟通，记录用户结论。
- [ ] 用代表性视频验证 Supadata 的已有字幕、无字幕生成、时间信息和异步任务行为。
- [ ] 核对实际额度消耗、错误情况和可访问视频范围。
- [x] 用户确认第一版只依据逐字稿整理，以稳定性为先；画面理解不纳入开发与验收。
- [ ] 验证中文翻译与 AI 内容梳理候选方案。
- [x] 用户确认 Mac 系统钥匙串保存 API key；按需组件负责凭据与受限请求，扩展负责业务编排，不做浏览器关闭后的本地长任务（仅方案）。
- [ ] 验证组件安装、Native Messaging、钥匙串授权、Chrome 重启后复用和签名升级；不要求手动启动或重复输入 key。

### 阶段 2：确认交互原型

- [ ] 设计加载、逐字稿、AI 内容及笔记视图。
- [ ] 设计三种语言模式、当前段高亮、自由阅读和底部跟随。
- [ ] 设计快捷键弹窗、划词入口及无常驻标签的 SVG 笔记界面。
- [ ] 设计来源更新提示、差异展示及保留或更新引用操作。
- [ ] 确认栏目名称和页面布局。
- [ ] 验证视频下方入口位置，明暗主题与窄屏布局。
- [ ] 验证主面板关闭时的快捷窗、无字幕缓存及切换视频的草稿体验。

### 阶段 3：打通逐字稿阅读流程

- [ ] 实现字幕获取、加载状态、失败重试和结果复用。
- [x] 实现本地分段、原文译文对应及语言切换（0.1.8 补充分句候选与巨段修复，真实 Chrome 效果待验收）。
- [ ] 实现播放高亮、自由滚动、恢复跟随、文字点击跳转播放。
- [ ] 实现逐字稿与译文编辑保存。
- [ ] 实现 YouTube 视频上下文识别、页面可见范围、跨视频和多标签页隔离。

### 阶段 4：打通笔记流程

- [ ] 实现当前播放片段的快捷键双语摘录。
- [ ] 实现划词摘录、想法和疑问输入。
- [ ] 实现当前电脑持久保存及视频时间关联。
- [ ] 实现摘录快照、来源修改提示、差异比较和用户选择更新。
- [ ] 实现已保存笔记再编辑、自动保存状态与编辑冲突处理。

### 阶段 5：实现 AI 内容与导出

- [ ] 实现默认中文的视频总结及整体结构。
- [ ] 实现各段问题、场景、金句、方法及切片建议。
- [ ] 实现 AI 内容到视频的时间关联。
- [ ] 实现完整逐字稿与个人笔记导出。

### 阶段 6：当前电脑端到端验收

- [ ] 使用真实样本完成下述验收，不以占位数据作为实际能力证明。
- [ ] 修复阻碍阅读、记录和保存的问题。
- [ ] 记录未覆盖的视频类型、外部服务限制及尚未完成项。
- [ ] 将每轮测试、缺陷修复和回归填写到 test-feedback.md。
- [ ] 用户对最终构建手动验收并明确通过，记录其原话和版本。

### 阶段 7：提交和推送

- [ ] 核对测试结果与人工验收对应同一最终构建。
- [ ] 确认 GitHub 目标仓库和可见性。
- [ ] 精确暂存代码与文档，使用 git diff --cached 检查敏感信息，不包含真实笔记或凭据。
- [ ] 创建 Git 提交，推送到确认的远程地址和分支，不使用强推。
- [ ] 记录提交哈希、推送结果、远程链接和剩余事项。后续代码变化须重新验证受影响范围。

## 六、第一版验收清单

- [ ] 三种逐字稿模式只显示所选内容；切换不重生成 AI 摘要。
- [ ] 有字幕与无字幕流程均有真实处理状态，失败或超额有明确提示。
- [ ] 播放高亮对应正确；自由浏览不被拉回；跟随按钮可在播放和暂停时定位。
- [ ] 单击段落会跳转并播放；拖动划词不会误触跳转。
- [ ] 快捷键摘录的是当前播放片段；划词摘录遵循最终确认的范围规则。
- [ ] 笔记保留双语、时间、理解和疑问；视觉标签符合简洁图标要求。
- [ ] 中文手动修改后不会被自动翻译覆盖。
- [ ] 修改来源后笔记引用不静默变化，用户能够比较并选择更新或保留。
- [ ] 更新引用不会修改个人理解与疑问。
- [ ] 刷新或重新打开视频后笔记可恢复。
- [ ] AI 框架有时间范围、内容介绍、问题及应用场景，金句和方法可回到来源核对。
- [ ] 导出完整逐字稿及笔记，双语和个人记录不丢失。
- [ ] 视频下方入口不重复、不遮挡；其他网页和 YouTube 非视频页没有页面 UI。
- [ ] A→B→A 切换、慢响应、自动连播及前进后退不串视频内容。
- [ ] 主面板关闭时快捷键仍能保存笔记；无缓存情况下先保留输入，引用补全正确。
- [ ] 已保存笔记可再编辑，刷新后修改仍在，草稿不因切换视频丢失或变更归属。
- [ ] 全部测试与人工验收真实记录，满足条件后才提交并推送。

## 七、服务调研依据

以下为需求讨论时查阅的官方资料，政策可能变化，实施阶段须复核；目前没有端到端接口测试结果。

- [Supadata 逐字稿说明](https://docs.supadata.ai/get-transcript)：支持视频链接、已有字幕获取、AI 生成及异步任务，返回分段时间信息。
- [Supadata 接口参数](https://docs.supadata.ai/api-reference/endpoint/transcript/transcript)：语言偏好不保证自动翻译；不可用时可能回退其他语言。
- [Supadata 价格](https://supadata.ai/pricing)：讨论时免费套餐为每月 100 credits，已有逐字稿每份 1 credit，AI 生成每分钟 2 credits；专用翻译接口属于付费功能。

## 八、后续版本

- 多用户使用及公开产品所需能力。
- 跨设备同步与更完整的备份恢复。
- 直接写入指定 Obsidian 知识库。
- 用户主动触发的个人笔记 AI 整理，保留原始摘录与个人记录。
- 第一版明确不包含实际短视频导出；后续是否加入另行评估。

## 七、已确认的节省额度策略

- 使用量：每周约 5 个视频，单个约 40 分钟、1 小时或更长，无固定上限。按月约 20–22 个是估算，不是用户提供的精确用量。
- 优先获取已有字幕并缓存；无字幕生成每次需先展示预计消耗并确认，不提供默认勾选或永久跳过，不自动切换为可能计费的生成模式。直接从 YouTube 获取的稳定性须验证，Supadata 是现有候选服务，不能把直接抓取写成已实现。
- 默认翻译尽量不消耗云端 LLM token；优先验证 Chrome Translator API。它不是浏览器整页翻译按钮，也不等于 Google Cloud Translation。
- 本地能力验证通过后用于中文/双语显示及双语摘录补齐；首次模型下载提供进度。不可用时保留原文和笔记，提示重试或主动选择 LLM，不自动付费回退。
- 段落提供“用大模型翻译”；全文操作独立展示范围与费用提示。结果作为候选译文，由用户选择替换，保留人工修订及笔记快照规则。
- AI 摘要、视频脉络等仍由用户主动触发 LLM；不把本地翻译称为整个插件完全免费。
- 在所有视频都有可获取字幕、每份只请求一次的假设下，Supadata 已有字幕约 20–22 积分/月；生成、失败请求、重复请求和 LLM 费用另计，价格变更以官方信息为准。

- 用户最新确认：保留引擎切换设计，先验证本地翻译质量；只有本地效果满足要求后再确定默认使用。本地不足时报告结果，等待用户明确指令才接入 client=gtx，当前不调用、不自动回退。

- 本地翻译验证结果：Chrome 独立扩展文档可调用，但 8 组定向样本存在口语及术语错译；见 [评估记录](local-translation-evaluation.md)。暂不确定默认引擎，client=gtx 等待用户指令。

## 最新实施安排：先完成产品，再在产品中评估翻译

用户已明确调整顺序：不再以独立翻译质量、速度对比作为开发前置关卡。两种免费方案均记录，首轮实现使用 Chrome 本地 Translator API，`client=gtx` 作为备选，LLM 保留主动翻译入口。以后根据插件中的实际体验决定是否切换，不自动更换默认引擎。

代码与构建状态统一记录在 [implementation.md](implementation.md)，实际缺陷及回归追加到 [test-feedback.md](test-feedback.md)。旧预检结果仅作为历史记录，不代表当前产品通过验收。

### 2026-09-11 设置页反馈落实

本轮增加删除连接、DeepSeek 自动填址与推荐配置、明确模型下拉列表及局部反馈，修复禁用按钮显示等待指针，并限制模型测试等待时间。代码检查完成；实际服务验收以 test-feedback.md 为准。

### 2026-09-11 设置页进一步调整

- 当前优先敲定设置，不继续视频功能验收。
- 已实现可编辑框一键清空、服务商选择与名称识别、对应地址和模型说明、字幕分模式费用卡与默认参考费率、统一灰绿导航及表单样式。
- 已核实本机 Codex 登录可由官方 CLI 复用；将“本机 Codex”记录为独立于 API 的连接方案，接入代码尚未实现。不能将 Codex 订阅伪装为 OpenAI API Key。

### 保存联动和 Codex 试用

用户要求本轮接入本机 Codex 供试用。已实现登录检测、模型测试、翻译与分析共用的本机适配器；普通 API 连接保留。新增保存快照比较，修复测试成功误报未保存及保存全部后局部文案不更新。实际验证结果见 test-feedback.md。

### Codex 名称显示修正

保留用户选择的 gpt-5.5，移除未使用的空白重复连接。模型分工和连接卡片明确显示模型名，禁止隐式默认模型；新增入口定位已有本机连接，防止重复添加。

### 应用标识补充（2026-09-11）

按用户参考图替换默认字母图标，使用黄橙粉渐变、白色播放符号、圆环及星光；覆盖工具栏、扩展管理页和页面标签。设置功能保持本轮已确认状态。

### 重载恢复补充（2026-09-11）

已实现旧内容脚本失效保护，完整流程验收前先验证扩展重载及视频页刷新后的恢复，实际结果见 test-feedback.md。

### 实际流程反馈修复（2026-09-11）

自动获取字幕和按显示模式本地翻译已接入，加载状态覆盖内容区；LLM 结果无冲突直接应用；长视频按原文长度分批整理；快捷笔记成功保存通知主面板更新。详细验证及实际浏览器待验收项见 test-feedback.md。

### 0.1.1 交付补充

每次代码交付升级版本。新增笔记删除、面板收起横条和可靠展开、工具栏恢复入口、独立阅读返回来源；移除面板大标题与常驻本地翻译按钮。视频脉络以模型选择来源编号、程序还原时间和原文的方式生成。详见 changelog.md 与 test-feedback.md。

### 0.1.2：展示与缓存反馈

- 面板向播放器顶部对齐，收紧标题区间距，不在正文标题区显示版本号。
- 加载内容整组居中，固定进度条及图标高度，避免受挤压变形。
- 字幕获取结果先由后台持久化，再返回面板；再次打开优先复用原文和译文。面板迟到结果不能覆盖已保存译文。
- 笔记正文统一字号，理解／疑问图标对齐首行；编辑和删除靠右成组。
- 视频脉络开头目标 2～3 句、约 100 字，长视频分批结果额外做一次简短总览，不重传整片原文。精简失败保留详细结果并折叠展示旧总结。

### 已确认：逐字稿分段基础方案（2026-09-12，0.1.7 已实现待验收）

用户确认以下基础方向，并要求先完成纯规则版本、查看实际效果后再讨论 SaT。0.1.7 已接入新获取字幕及旧缓存的显式预览入口；不自动修改旧缓存。后续版本记录中的“分段待确认”描述保留为当时状态。

1. 统一处理所有字幕来源：YouTube 直接字幕、Supadata 已有字幕和生成字幕进入同一套本地重组流程，再翻译和展示。
2. 统一重组：原始字幕条目不等于句子，允许跨条目合并、在条目内部重新断句；保留原始文本及来源时间映射。
3. 可靠断句：完整句优先，处理缩写、小数、姓名和引号等误切情况。可靠说话人变化阻止跨人合并；没有说话人数据不猜测身份。字幕时间间隔只能辅助判断，不能冒充真实语音停顿。
4. 向后比较：比较后续完整句的多种组合，减少残余短段；句子完整性和可靠说话人边界优先于段落长度均匀。
5. 长度软目标：约 220 英文原文字符作为待验证参考，不是硬上限；长句允许超过目标，短句可以组合，真实短回应可以独立。字符数、时长均不得单独触发句中硬切。

当前无新增依赖，使用本地标点规则和动态规划；每个起点最多比较后续 32 个完整句，英文／中文软目标分别为 220／100 字符。参数为实施初值，仍需真实视频调优。SaT 按用户要求暂不引入；音频说话人识别和逐段人工合并／拆分入口继续延后。缺标点且无法确认边界时优先保留较长段落，不补写原话。

后续验证使用真实字幕检查破句、过度合并、漏字重字、时间定位和本机开销。字幕只给整条时间时，句内跳转只能使用来源字幕精度，不能宣称逐词精确。升级需保护手改内容、译文和笔记快照，明确新旧映射，不直接覆盖旧引用。

### 0.1.3 视觉反馈

按用户标注将视频标题移到设置／收起所在首行，并规整设置齿轮；分段规则保持待确认，不随本次样式更新变更。

### 0.1.4 笔记卡片

参考用户提供的笔记截图调整引用层次、时间位置与卡片外观，保留已有理解／疑问、编辑、跳转、删除功能和统一字号。标题上移及设置图标调整一并包含；分段仍待确认。


## 0.1.6 笔记图标（2026-09-11）

笔记卡片与编辑窗口共用的理解、疑问 SVG 改为用户选定的笑脸黄色灯泡与腮红粉色问号气泡。实际 Chrome 外观待用户验收。

### 0.1.5 操作与导出补充

按用户最新截图继续上移并对齐视频；跟随逻辑不变，入口置右下角。语言改为直接三选一，LLM 翻译仅保留整片操作并简化名称。导出支持三类内容任意组合与 Markdown／TXT／DOCX／PDF；PDF 经打印另存。全片简短总结和主题介绍分开展示，保留独立阅读。分段方案仍待确认。


### 已实现待验收：三组真实稿件的分段修复（2026-09-21，0.1.8）

用户授权按复核发现补齐规则。短引用保护限制为 240 字符；超长单元搜索保守的英文分句线索，保留原话，不按定长硬切；识别经过上下文确认的 `>>` 轮次。超过两倍软目标仍无法缩短的段落提示检查。推断分句不等同于已确认完整句，纯规则仍需人工检查衔接。

分段算法升级为版本 2，从旧记录的来源范围恢复细粒度时间；继续使用显式预览／应用／恢复，手改段落和笔记保持，未变段落保留译文。三组稿件回放分别为 93、397、95 段，最长英文分别为 594、563、319 字符。SaT 仍延后；真实 Chrome 时间联动与用户阅读体验待验收。具体结果见 [修复记录](segmentation-repair-2026-09-21.md)。


### 已确认：保留本地分段并新增可修改快捷键（2026-09-21，0.1.9）

用户确认三个复测视频的分段效果符合预期，其他视频未测试；继续使用现有本地规则，SaT 仅作为后续需要时重新讨论的备选。本轮不修改分段算法。

新增独立的“打开学习笔记”和“快速记笔记”快捷操作，不要求先点击页面按钮。设置页展示 Chrome 实际绑定值，提供修改入口。Chrome 公开 Commands API 无直接更新绑定的方法，修改在 Chrome 快捷键页完成并由浏览器自动保存；返回设置页刷新实际值，不在本地另存一份无效绑定。默认打开为 Command+Shift+U／Ctrl+Shift+U，笔记保留已有 Command+Shift+Y／Ctrl+Shift+Y；用户已有映射不主动覆盖。新构建快捷键真实 Chrome 交互仍待验收。


## 0.1.10 AI 视频脉络调整（待人工验收）

按用户确认：适用场景展开为具体处境和用法；切片仅做全片总判断与每段独立性粗判断，详细切片规划后续再做；增加知识清单与独立前置知识块。金句用时间点击跳转和淡色双页复制图标，默认复制双语，可选中文或原文。旧结果保留，重新整理后生成新增内容。已用 Jessica Wu、Rebecca 两组真实视频逐字稿调用当前模型验证；Chrome 完整交互与用户内容质量验收仍待完成。


## 0.1.11 分析格式错误修复

已复现真实模型把金句分类写成“方法洞察”，严格枚举导致整批分析失败。现保留来源有效的金句、省略未约定标签并提示；真正结构错误报告具体栏目与字段，不回显模型原文。原文与引用校验不放宽。用户指定视频两批真实分析通过，真实失败回复回放及 125 项单元测试通过；本机组件已更新，Chrome 用户复测待完成。


## 0.1.12 阅读、笔记与 AI 提问（待 Chrome 验收）

三个分析栏目标题加粗并分条；主题时间与标题同排；知识来源时间合并相邻片段、其余来源折叠。“脉络”移到跟随左侧，移除重新分段和补齐翻译测试入口，获取字幕仍仅用新版规则。金句保留此前四类，增加 clipboardWrite 权限和 iframe 剪贴板授权及自动备用复制。逐字稿／分析划词下方显示淡色 note 与 AI提问；知识点可加入笔记，笔记可向当前分析模型提问。问题先存草稿，AI 回答独立保存并导出，不覆盖个人理解。

实现及逐项验证见 [阅读与 AI 提问记录](learning-workflow-2026-09-21.md)。136 项单元测试、真实问答与结构化生成、浏览器复制及选区动作已验证；Chrome 全链路与视觉待用户确认。


## 0.2.0 阅读与笔记精简

按用户确认实施：模块标题 16px，脉络小标题与正文 15px，知识说明标签加粗；划词只保存选中摘录，不自动扩展双语逐字稿，知识卡移除常驻操作。Note 使用“我的理解与价值”和“我的疑问”，底部删除、取消、AI提问、完成同排；取消保存当前草稿，完成结束草稿状态。金句采用反直觉洞察、点透本质、方法与原则、关键事实、案例与经验五类，兼容旧标签。验证结果见 test-feedback.md。


## 0.2.1 知识条目与可编辑摘录

知识清单的需要理解、视频中的作用生成数组并分条展示，兼容旧文本。划词记录保留标题、段落、列表、加粗结构，笔记摘录可直接编辑并自动保存；不修改逐字稿，修改后标记手动编辑。字号问题须区分源码设置与用户实际页面，不能仅凭截图声称 CSS 精确值。

0.2.1 字号核实：真实 Chrome 扩展独立面板主题 15px、正文 11.25px，原因是 body 注入 75% 字号；此前同字号结论已纠正。当前保留实测值并优化时间／标题横排，进一步字号调整待用户选择。


## 0.2.2 用户确认的阅读排版

逐字稿中英文（包括单语模式）统一字号 12px、行高 20px；段落时间保持 12px。全片总结、全片切片判断、知识清单、金句、有效方法五个模块标题统一为 16px／600／26.72px，前加 4px × 18px 的主题色竖条。片段、知识点和有效方法小标题统一为 15px／600／22.5px。其余正文、标签和前置知识保持原样。版本统一 0.2.2；本次仅排版调整，不重新生成 AI 内容。


## 0.2.2 阅读样式确认与笔记核查

用户已认可当前脉络字号与层级，先保持。完整参数归入 design.md 的“2.2 设计样式”，作为后续修改入口；笔记卡片与编辑表单已核查，列出实际值和来源字号例外，等待用户指定调整参数。此轮仅文档更新与读取检查，不改产品样式、不升级版本。


## 0.2.3 笔记排版确认

用户确认折叠标题行高 18.79px，个人字段标签 600、输入文字 400；按核对的完整方案统一卡片与编辑器文字、顶部图标与时间布局、旧新摘录字号。样式目标同步 design.md。仅调整呈现，笔记内容与来源信息保留。


## 0.2.4 编辑视觉与回答要求

用户要求提高编辑区辨识度并考虑回答设定。样式按反馈落实；回答设定先按推荐默认方案实现，支持本次编辑、显式保存默认、恢复内置。等待用户进一步细化偏好。


## 0.2.5 笔记内层装饰精简

将 AI 提问的圆角边框、左侧强调线与内边距限定为编辑表单样式。笔记展示卡片不再显示内层框线；外层笔记卡片、编辑表单、折叠交互和文字参数保持原样。仅 style.css 选择器范围、五处版本与说明文档更新。


## 历史记录与知识库出口：0.3.0 实施

历史页采用左列表、右阅读，搜索包含标题及笔记文字。第一版不接知识库；Obsidian 优先、飞书精选，以及筛选精排或先入待整理文件夹交给 Agent 的两条路径仅记录为后续需求。用户已确认合并独立阅读入口、AI 回答不参与搜索且不提供可选搜索开关、历史页允许继续编辑及提问，详见 [历史记录讨论稿](history-records-design.md)。2026-09-23 用户确认历史记录功能可用并同意提交推送；样式后续继续微调，本次不改代码或版本。
