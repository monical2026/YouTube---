# YouTube 学习笔记插件

用于在 Chrome 中阅读 YouTube 视频逐字稿、查看中文 AI 内容梳理并记录双语笔记。第一版只依据逐字稿翻译和整理，不分析视频画面；面向个人在当前电脑使用，后续再扩展多用户和跨设备能力。

插件图标采用黄橙粉渐变与白色播放符号，资源说明见 [设计文档](DOC/design.md)。

打开视频面板后自动获取字幕，中文／双语模式自动本地翻译；LLM 翻译由用户触发并直接应用无冲突结果。长视频脉络支持分批整理，快捷笔记保存后通知主面板刷新。

当前版本：**0.3.0**。本次变化见 [版本更新记录](DOC/changelog.md)。字段字体、字号、字重与行高集中查阅 [设计样式](DOC/design.md#22-设计样式字段与排版参数当前-025)。

0.1.8 修复长引号导致巨段、缺标点长段没有备用断点的问题，补充会话轮次识别和偏长提示；继续采用本地规则，不引入 SaT。旧视频点击“重新分段”预览并应用，改变边界后点击“补齐本地翻译”；可在预览窗口恢复上次逐字稿及译文。三组用户视频的分段效果已获用户确认符合预期，其他视频继续观察，详见 [修复记录](DOC/segmentation-repair-2026-09-21.md)、 [实施计划](DOC/PLAN.md) 和 [测试反馈](DOC/test-feedback.md)。

0.1.9 增加打开面板快捷键与设置页“快捷键”分类。默认打开面板为 ⌘⇧U／Ctrl+Shift+U，快速记笔记为 ⌘⇧Y／Ctrl+Shift+Y；实际绑定以设置页显示为准。点击“修改快捷键”进入 Chrome 修改并自动保存，返回时刷新显示，不依赖本机组件连接。

历史记录将“独立阅读”合并为一个入口：逐字稿工具行右侧点击“历史记录”，或在非视频页点击插件图标。历史页可脱离原视频阅读本地逐字稿、脉络和笔记，搜索标题与笔记摘录、理解、疑问，继续编辑或主动向 AI 提问。本版不对接知识库。

## 当前状态

已进入产品开发：pnpm 工作区、Chrome 扩展页面、YouTube 页面适配、逐字稿与笔记存储、翻译入口、AI 梳理入口、服务设置和 macOS 钥匙串组件已有首轮实现，构建产物位于 `extension/dist/` 和 `service/dist/`。

0.2.5 已获用户第一版基本可用验收；0.3.0 历史记录功能已于 2026-09-23 获用户验收；后续继续微调样式。用户已确认三组视频分段效果与 0.1.9 快捷键功能；其他验收范围按测试反馈分别记录。代码检查和实际验证进度见 [实施记录](DOC/implementation.md) 与 [测试反馈](DOC/test-feedback.md)。已停止独立翻译通道对比测试，后续在本插件中评估。源码仓库：[monical2026/YouTube---](https://github.com/monical2026/YouTube---)。

设置页提供连接删除、输入框一键清空、DeepSeek／智谱／OpenAI 服务商匹配、模型列表和测试反馈；字幕按获取方式展示费用，默认提供生成参考费率。已加入本机 Codex 登录连接入口，可检测登录、测试模型并用于模型分工；连接保存状态与测试结果独立显示。实际验证状态见测试反馈文档。

## 计划功能

- 原文、中文、中英双语逐字稿，支持播放高亮、自由浏览、跟随和点击跳转。
- 随 YouTube 视频切换更新内容，其他网页不注入插件界面。
- 面板关闭时快捷记笔记，支持划词摘录、后续编辑和来源版本更新提示。
- 默认中文的摘要、视频脉络、金句、方法与切片建议。
- 独立设置网页，配置 LLM、Supadata 等已适配服务及任务模型。
- 当前电脑保存，支持逐字稿和笔记导出；直接写入 Obsidian 放在后续版本。

## 文档导航

| 文档 | 内容 |
| --- | --- |
| [AGENTS.md](AGENTS.md) | 项目级协作说明、工作顺序和约束 |
| [实施计划](DOC/PLAN.md) | 已确认需求、实施默认、待确认项和阶段清单 |
| [文件架构](DOC/architecture.md) | 工作区、完整文件树和依赖边界 |
| [界面设计](DOC/design.md) | 布局、主题、字号、交互和界面状态 |
| [技术方案](DOC/technical-design.md) | 视频上下文、数据存储、任务和安全方案 |
| [服务设置](DOC/service-settings.md) | 独立设置页、LLM/API 连接及安全凭据管理 |
| [测试方案](DOC/test-plan.md) | 测试用例、真实集成与人工验收 |
| [测试反馈](DOC/test-feedback.md) | 待填写的测试、缺陷、修复、回归和交付记录 |

## 目录约定

`README.md` 与 `AGENTS.md` 放在根目录，项目说明性文档放在 `DOC/`。源码按已确认方案分为 `extension/`、`service/`、`shared/`，测试放在 `tests/`。

## 安装与运行

当前使用 pnpm 11.19.0、Node.js 26.3.1（本机开发环境）、TypeScript 6.0.3、React 19.3.0、Vite 8.3.0。版本以根 `package.json` 和 `pnpm-lock.yaml` 为准。TypeScript 6 用于兼容当前 ESLint 工具链。

```sh
pnpm install --frozen-lockfile
pnpm check
```

`pnpm check` 依次执行类型、lint、格式、业务单元测试、扩展与 Swift 钥匙串组件构建。Mac 需安装可运行 `swiftc` 的命令行开发工具。检查不包含 Chrome 真实交互验收。

1. 打开 Chrome 的 `chrome://extensions`，开启开发者模式，选择“加载未打包的扩展程序”，选择本项目的 `extension/dist`。
2. 复制该扩展的 ID，执行 `pnpm native:install 扩展ID`（替换最后一项）。该脚本把本机构建组件复制到当前用户的 Application Support，并仅授权这个扩展 ID，不读取或写入 API key。
3. 打开扩展的设置页，添加服务、填写 API key 和模型，选择翻译／分析／字幕对应的服务，再保存。Key 只写入 macOS 钥匙串；系统授权窗口由本人处理。
4. 刷新一个普通 YouTube 视频页，点击视频下方“学习笔记”。首次使用先获取已有字幕，再在需要时启动本地翻译。
5. 在插件“设置 → 快捷键”查看和修改“打开学习笔记”“快速记笔记”两个操作。Chrome 修改页自动保存；显示未设置时需自行指定按键。

只用已有 YouTube 字幕、本地翻译和笔记时，不需要 LLM key；Supadata 和 LLM 入口需要实际配置相应服务。没有字幕生成计价规则时不允许提交生成；每个新生成操作仍需单独确认。

本机组件当前使用注册时的 Node 可执行文件路径，适合这台开发电脑；面向其他电脑的运行时打包与签名尚未完成。源码重新构建后，需重新加载扩展，并刷新已打开的 YouTube 视频页以载入新版脚本；本机组件修改后还需再次运行登记命令。

## 验收与交付

按测试方案完成代码验证，将真实结果记录到测试反馈文档。全部适用测试通过且用户明确人工验收通过后，再执行 Git 提交及 GitHub 推送。目标仓库和可见性尚未确认。

当前凭据方案：macOS 系统钥匙串 + Native Messaging 按需组件。实现已写入，首次真实密钥保存、系统授权和重启复用仍须在产品设置页验证。

翻译策略：先采用 Chrome 本地 Translator API；`client=gtx` 保留为免费云端备选，待插件实际使用反馈后按用户指令切换。LLM 翻译主动点击才执行，本地失败不自动产生付费请求。两种免费引擎未得出整片质量或速度的对比结论。

页面范围：普通 YouTube watch 点播页；Shorts、正在直播及其他网站嵌入的播放器不属于第一版范围。


## 0.1.10 AI 视频脉络调整（待人工验收）

按用户确认：适用场景展开为具体处境和用法；切片仅做全片总判断与每段独立性粗判断，详细切片规划后续再做；增加知识清单与独立前置知识块。金句用时间点击跳转和淡色双页复制图标，默认复制双语，可选中文或原文。旧结果保留，重新整理后生成新增内容。已用 Jessica Wu、Rebecca 两组真实视频逐字稿调用当前模型验证；Chrome 完整交互与用户内容质量验收仍待完成。


## 0.1.11 分析格式错误修复

已复现真实模型把金句分类写成“方法洞察”，严格枚举导致整批分析失败。现保留来源有效的金句、省略未约定标签并提示；真正结构错误报告具体栏目与字段，不回显模型原文。原文与引用校验不放宽。用户指定视频两批真实分析通过，真实失败回复回放及 125 项单元测试通过；本机组件已更新，Chrome 用户复测待完成。


## 0.1.12 阅读、笔记与 AI 提问（待 Chrome 验收）

三个分析栏目标题加粗并分条；主题时间与标题同排；知识来源时间合并相邻片段、其余来源折叠。“脉络”移到跟随左侧，移除重新分段和补齐翻译测试入口，获取字幕仍仅用新版规则。金句保留此前四类，增加 clipboardWrite 权限和 iframe 剪贴板授权及自动备用复制。逐字稿／分析划词下方显示淡色 note 与 AI提问；知识点可加入笔记，笔记可向当前分析模型提问。问题先存草稿，AI 回答独立保存并导出，不覆盖个人理解。

实现及逐项验证见 [阅读与 AI 提问记录](DOC/learning-workflow-2026-09-21.md)。136 项单元测试、真实问答与结构化生成、浏览器复制及选区动作已验证；Chrome 全链路与视觉待用户确认。


## 0.2.0 阅读与笔记精简

按用户确认实施：模块标题 16px，脉络小标题与正文 15px，知识说明标签加粗；划词只保存选中摘录，不自动扩展双语逐字稿，知识卡移除常驻操作。Note 使用“我的理解与价值”和“我的疑问”，底部删除、取消、AI提问、完成同排；取消保存当前草稿，完成结束草稿状态。金句采用反直觉洞察、点透本质、方法与原则、关键事实、案例与经验五类，兼容旧标签。验证结果见 test-feedback.md。


## 0.2.1 知识条目与可编辑摘录

知识清单的需要理解、视频中的作用生成数组并分条展示，兼容旧文本。划词记录保留标题、段落、列表、加粗结构，笔记摘录可直接编辑并自动保存；不修改逐字稿，修改后标记手动编辑。字号问题须区分源码设置与用户实际页面，不能仅凭截图声称 CSS 精确值。

字号纠正：已安装 Chrome 0.2.0 扩展独立面板实测标题 15px、正文 11.25px，原“均为 15px”来自独立组件页，不适用于真实扩展。0.2.1 显式保留这组实际字号，并收紧时间标签、采用右侧标题独立换行；后续字号选择待用户确认。

## 0.2.2 阅读字号统一

逐字稿中英文统一 12px／20px 行高，时间保持 12px。五个脉络模块标题统一 16px／600／26.72px，并加主题色竖条标识；片段、知识点和有效方法标题统一 15px／600／22.5px。其余文字参数保持原状。

## 0.2.3 笔记文字层级统一

笔记标识与时间居左，增加书本及链接图标；摘录和 AI 回答统一 13px，个人内容 12px。编辑与卡片共用问答样式，个人字段标签 600、输入文字 400；折叠标题行高 18.79px。去掉 AI 梳理摘录显示标签，来源信息保留。

## 0.2.4 编辑视觉与回答设定

排版工具提高文字对比，手动编辑状态右置，个人字段增加图标及括号选填。AI 回答常见章节加标识；AI提问内可编辑本次回答要求、保存为默认或恢复内置要求，设定保存在本机，发送时才附给模型。

0.2.5：笔记展示卡片内的 AI 提问去掉圆角边框和左侧强调线，编辑表单保留原有装饰；折叠与文字样式不变。
