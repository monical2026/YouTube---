# 项目级协作规则

适用于本仓库。遵守当前有效的上级指令和用户最新要求；设计默认不是用户亲定结论。本文保持不超过 250 行，详细方案放 DOC/，不通过超长单行绕过限制。

## 一、当前状态与目标

- 目标：个人使用的 Chrome YouTube 学习笔记插件，提供双语逐字稿、中文 AI 梳理、可编辑笔记和导出。
- 无字幕生成每次展示预计消耗并经明确确认；确认绑定视频与操作，禁止 auto 绕过；快捷摘录先保存草稿，取消不丢输入。
- 普通翻译首轮实现选 Chrome 本地 Translator API，client=gtx 保留备选；停止独立对比测试，在实际插件中评估；LLM 翻译必须主动触发，不因本地失败自动付费回退；候选译文应用前检查修订。
- 第一版支持普通 YouTube watch 点播页；Shorts、正在直播、站外嵌入后续再做。范围外不挂 UI、不触发字幕/AI/快捷笔记，保留已有草稿。
- 第一版仅依据逐字稿翻译和整理；不采集画面、不做 OCR 或视频理解，不猜测教程中只在画面出现的步骤。
- 已建立 pnpm 工作区及首轮产品实现，可构建 Chrome 扩展和本机组件；状态见 DOC/implementation.md。实际浏览器联调、真实凭据与服务验收尚未完成；不要把已有独立验证夹具当产品验收。
- 接手先检查文件及 `git status --short`，以实际状态为准；开始实施后同步 README.md 和 DOC/PLAN.md。
- 用户只要求文档时不启动开发，不预先创建空模块冒充实现。

## 二、可作为上下文的文档

| 顺序／场景 | 文档 | 主要用途 |
| --- | --- | --- |
| 首先阅读 | [README.md](README.md) | 当前状态、项目入口 |
| 确认做什么 | [DOC/PLAN.md](DOC/PLAN.md) | 已确认需求、实施默认、待确认项与阶段 |
| 确认代码放哪 | [DOC/architecture.md](DOC/architecture.md) | 工作区、文件树和模块依赖 |
| 修改界面 | [DOC/design.md](DOC/design.md) | 布局、字号、主题、交互状态 |
| 修改业务逻辑 | [DOC/technical-design.md](DOC/technical-design.md) | 视频切换、任你说的是每次如果我把这个浏览器给关了，然后再重新启动打开这个插件的时候，我打开一个新的视频让它翻译的时候都要重新去输入这个API Key吗？务、存储和版本规则 |
| 接入模型／API | [DOC/service-settings.md](DOC/service-settings.md) | 设置网页、服务配置、模型路由和凭据 |
| 实现前与验证时 | [DOC/test-plan.md](DOC/test-plan.md) | 用例、边界、真实服务与人工验收 |
| 当前实现与差距 | [DOC/implementation.md](DOC/implementation.md) | 代码入口、实际构建状态、待完成项 |
| 记录实际结果 | [DOC/test-feedback.md](DOC/test-feedback.md) | 问题、根因、修复、回归、剩余项与签收 |

文档冲突时核对用户最新要求并同步修改，不挑方便的一份执行。根目录放 README.md、AGENTS.md；其他说明文档放 DOC/。

## 三、包管理器与检查命令

- **统一使用 pnpm + pnpm workspaces**；不要混用 npm、Yarn、Bun 安装或升级依赖。
- 根目录使用 `pnpm-workspace.yaml` 声明 extension、service、shared，只保留一个 `pnpm-lock.yaml`。
- 不生成 package-lock.json、yarn.lock 或子工作区锁文件；发现旧锁文件先说明，不直接删除。
- 初始化时验证 Node.js/pnpm 版本，写入 package.json 的 `engines.node`、准确的 `packageManager: pnpm@版本号` 和 README；目前不虚构版本。
- 已有锁文件且不改变依赖时执行 `pnpm install --frozen-lockfile`；首次初始化执行 `pnpm install`。
- 增加依赖用 `pnpm --filter <工作区包名> add <包名>`；开发依赖加 `-D`，根工具明确使用 `-w -D`。占位符须换成实际名称。
- 新依赖必须说明当前用途、现有能力为何不足；不顺带升级、不删锁文件解报错、不自动放开依赖安装脚本。
- 只用项目锁定的检查工具，不临时下载未固定版本工具绕过配置。纯文档改动不安装依赖。

下列为检查命令约定；typecheck、lint、format:check、test:unit、build、check 已落实，integration/e2e 尚待加入，不能声称已运行：

| 根目录命令 | 验证内容 |
| --- | --- |
| `pnpm run typecheck` | 全部已建立工作区的 TypeScript 类型 |
| `pnpm run lint` | 未使用代码、显式 any、异步错误和 Hooks 规则 |
| `pnpm run format:check` | 统一源码和配置格式 |
| `pnpm run test:unit` | 真实单元测试，无用例不算通过 |
| `pnpm run test:integration` | 数据、消息与服务契约 |
| `pnpm run test:e2e` | 扩展、设置页、视频流程 |
| `pnpm run build` | 扩展和本机组件构建 |
| `pnpm run check` | 类型、lint、格式、单元测试、构建依次执行；任一步失败即失败 |

禁止以 `--if-present`、`|| true`、空脚本、删断言或关规则伪装通过。check 不代替集成、端到端或人工验收；缺环境如实记阻塞。

## 四、核心业务流程

1. **配置服务**：独立设置页 → 检测按需组件与系统钥匙串 → 配置 Supadata 和 LLM → 测试指定模型 → 安全保存；翻译与分析可分别选模型。
2. **识别视频**：仅在支持的 YouTube 视频页挂入口；打开面板时确定 videoId、标签页和导航代次，读取该视频缓存。
3. **取得逐字稿**：获取已有字幕；没有时按允许的额度策略生成 → 本地分段并保留时间映射 → 按原文／中文／双语模式处理展示。
4. **播放联动**：当前段高亮；手动滚动停止自动拉回；“跟随”只定位文本；单击段落跳转视频并播放，选词和编辑不触发跳转。
5. **快速记笔记**：快捷键冻结当前视频与时间，面板关闭也可用；划词保留选区来源。先保存草稿，无字幕时异步补齐双语，不丢个人输入。
6. **编辑与修订**：完成草稿成为正式笔记；旧笔记可再编辑。来源变化只提示差异，用户选择更新引用，理解与疑问不被自动改写。
7. **AI 梳理**：用户触发中文摘要、视频脉络、金句、方法和切片建议，保留引用时间；切换逐字稿语言不重新生成；切片仅给建议时间范围和理由，不剪辑或导出视频文件。
8. **保存与带走**：本地持久化成功后显示已保存，导出完整稿件或笔记；直接写入 Obsidian、跨设备和多用户属于后续版本。
9. **切换视频**：先撤下旧内容并让旧界面请求失效，再载入新视频；旧草稿仍绑定原视频，迟到结果不得串入新界面。

组件由 Chrome 经 Native Messaging 按需启动，不开放 HTTP 端口、不做配对码、不要求手动启动；全片任务由扩展分批编排并保存进度，关闭浏览器不要求继续运行。重启后不重复输入 API key，系统授权弹窗不得绕过。

## 五、最高风险模块与必测事项

以下是基于设计的风险评估，部分细分目录尚未拆出，入口见 DOC/implementation.md；不代表已发现缺陷。

| 风险等级 | 模块 | 出错后果 | 修改后必须验证 |
| --- | --- | --- | --- |
| 最高 | `service/src/credentials/`、`security/`、`extension/src/background/message-router.ts` | 密钥泄漏、越权配置或任意网络代理 | 非法来源、系统授权、脱敏、目的地址限制 |
| 最高 | `extension/src/storage/`、`ui/notes/` | 笔记丢失、手改被覆盖或迁移损坏 | 写入失败、刷新重启、取消编辑、来源差异、双标签冲突 |
| 最高 | `extension/src/content/navigation/`、`background/context-registry.ts` | A 视频结果写入 B、记录到错误时间 | A→B→A、乱序响应、多标签和草稿归属 |
| 高 | `content/youtube-adapter/`、`segmentation/` | 广告时间当正片、分段和跳转错位 | 广告、节点替换、倍速、时间边界、粗时间戳 |
| 高 | `service/src/providers/`、`requests/`、`extension/src/jobs/`、`pipelines/` | 重复计费、无限重试、翻译缺段或虚构引用 | 超额限流、未知提交结果、恢复、重复任务、响应校验 |
| 高 | `service/src/config/`、`extension/src/options/` | 模型选错、配置中途混用、密钥发往错误地址 | 配置版本、模型实测、连接失败不覆盖旧配置 |

## 六、哪些目录和文件不能随便修改

“谨慎修改”不是禁止完成相关任务；修改前说明影响，读取直接调用方，修改后执行对应回归。无关任务不得顺手重构这些位置。

| 位置 | 约束 |
| --- | --- |
| `shared/src/schemas/`、`types/` | 改协议需同步发送方、接收方及契约测试，不能只改一侧 |
| `extension/src/storage/migrations/` | 不重写已发布迁移；新增迁移并验证旧数据升级，禁止清库替代修复 |
| `service/src/credentials/`、`security/`、`native/keychain/` | 不放宽来源校验或降级明文保存；不得临时打印密钥排错 |
| `extension/manifest.json` | 改权限、页面匹配、设置入口或快捷键须说明理由并验证非视频页不受影响 |
| 根 package.json、pnpm-workspace.yaml、pnpm-lock.yaml、构建配置 | 仅因当前任务需要修改；不切换包管理器、不批量升级、不关闭检查 |
| `tests/`、`DOC/test-plan.md`、`DOC/test-feedback.md` | 不删失败测试换通过，不覆盖历史证据；预期变化必须有需求依据 |
| `.git/`、构建 dist/、node_modules/ | 不直接改 Git 内部数据、生成产物或安装包；修改源文件再生成 |
| 项目外应用数据、系统凭据库 | 不把重置、删除或迁移作为默认排障；先明确数据影响及用户授权 |

## 七、可执行的编码规则

### 干净：职责、类型和命名清楚

- 按架构分层，组件不持密钥或直连供应商；shared 不依赖 React、Chrome API 或 Node 文件系统。
- TypeScript 开启 strict；外部输入先用 unknown 并校验，禁止 any、双重断言或 @ts-ignore 绕过错误。
- 标识符用英文且含业务含义，时间明确 startMs/endMs，边界转换单位；不混用秒与毫秒。
- 清理本次引入的调试输出、未使用代码和注释掉的旧实现。用类型、lint、非法输入测试及 diff 检查。

### 简洁：只为当前需求增加结构

- 函数完成一个明确动作；获取字幕、控制播放器、保存笔记不要堆在同一个函数。
- 手写源码文件超过 300 行、函数超过 60 行或嵌套超过 3 层时审查拆分；保留须写理由，不压缩语句凑行数。生成文件和夹具不适用。
- 新公共工具先找现有实现并列实际调用方；不预建未来功能的空接口和目录。
- 同一业务规则只保留一个实现来源；通过调用关系、重复逻辑搜索和变更说明检查。

### 不乱改：每个文件都与任务有关

- 修改前读目标文件、直接调用方及 git status；检查未跟踪文件，不以空 diff 当作无改动。
- 不顺手全仓格式化、改无关命名、主题、依赖或协议；扩大范围先说明原因。
- 保留用户已有工作；不使用 reset --hard、checkout -- .、git clean 清除改动。删除、移动遵守明确授权。
- 改接口或数据结构同步契约、兼容/迁移与文档。交付时逐个解释变更文件的用途。

### 性能：避免重复工作并实际测量

- timeupdate、滚动和动画帧内不发 API、不重翻译、不扫描全部笔记或整库写入；用有序时间索引定位。
- 按视频、来源版本、模型配置和任务类型复用缓存及在途任务；重复操作检查实际请求次数。
- 监听器、observer、订阅、计时器都有 dispose 或 Effect 清理；A/B 切换 20 次不累积处理次数。
- 按测试方案记录同环境、同数据量的耗时；300ms 是拟定交互目标，不是已达成绩，不无差别添加 memo。

### 稳定：失败有边界，个人数据不丢

- 异步入口有错误处理，禁止空 catch、未处理 Promise 或空数组冒充成功；void 本身不算处理失败。
- 外部请求有超时；安全重试默认最多追加 2 次；429 受任务期限约束，鉴权/参数错误不重试。
- 可能计费的提交状态未知时先查任务，不盲目重发；任务停止与取消等待不等于供应商停止计费。
- 回填检查视频、导航代次和版本；写笔记检查修订号；持久化成功才报已保存，失败保留输入。
- 核心逻辑缺陷补可复现回归再修复；视觉缺陷保留同场景证据。简单文档和不涉及行为的低影响样式不强制新增代码测试。

## 八、安全、浏览器与交付

- 日常 API key 用 macOS 系统钥匙串，环境变量仅为开发/受管来源，不硬编码、不放命令行参数、不写日志/普通配置/导出；不进入 YouTube 内容脚本。
- 个人理解和疑问不自动外传；.gitignore 排除真实 .env、私钥、凭据和私人测试数据，.env.example 仅占位符。
- 浏览器自动化默认 Ego Lite，先读 ego-browser 技能；不可用先说明原因。产品目标仍为 Chrome，不冒用 Ego Lite 结果当 Chrome 验收。
- Cookie 和登录凭据只由浏览器本机导入，不导出明文；系统密码由用户在系统窗口输入。
- 按改动执行检查，纯文档只查结构、链接和一致性；通过、失败、阻塞、未执行分别报告，不以模拟服务代替真实服务。
- 将实际测试环境、问题、根因、修复、回归及下一步追加到 DOC/test-feedback.md；未知原因标假设，不填虚构通过。
- 全部适用测试及用户对最终构建的人工验收通过后，才提交和推送；改动已验收构建后重新验证。
- 发布前确认 GitHub 目标及可见性；新分支默认 codex/，不强推，不自行建公开仓库。
- 精确暂存后检查 git diff --cached，确认无秘密和无关变更；记录实际 commit、推送结果及限制。

## 九、版本迭代

- 每次交付包含代码修复或功能变化的新构建，至少递增补丁版本；纯文档修改无需升级。
- 根 package.json、三个工作区 package.json 与 extension/manifest.json 的版本必须一致，build 会校验；在 DOC/changelog.md 记录本次变化及待验收项。
